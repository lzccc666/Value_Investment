# 012_数据管理

## 模块定位

012 是本地 SQLite 的独立运维模块，不属于 `004-011` 研究主链路。概览页保持 `004-011 / 8 / 8`；侧边栏提供独立“数据管理”入口，页面标题为“数据管理”。

## 安全协议

破坏性操作采用两阶段协议：

1. Preview 根据当前数据库生成各表影响数量、受保护记录、预计释放空间、确认短语和十分钟有效的一次性令牌。
2. Execute 同时提交令牌和精确确认短语。后端重新计算数据库状态摘要；令牌过期、重复使用或数据库状态变化都会拒绝执行。

令牌绑定操作类型、参数和数据库状态。维护闸门保证同一时刻只有一个维护操作；进入维护后等待已有写请求结束，并让新的普通写请求返回 `503`。并发维护返回 `409`。破坏性数据库操作默认先创建并校验备份。

## 清理语义

### 单家公司

保留 `Company` 的证券身份、档案和行情，按依赖顺序删除该公司的 `PriceDecisionRun`、`ValuationRun`、`InvestmentMemo`、`AnalysisRun`、`Evidence`、`Announcement` 和 `FinancialStatement`，不影响其他公司。

### 全局分析历史

保留全部 `Company`、`FinancialStatement`、`Announcement`、`Evidence`，同时保留 Evidence 和公告诊断运行。只删除 `run_type=analyst_view`、`run_type=investment_memo` 及全部 Memo、估值和价格决策。

### 版本保留

支持单公司或全部公司，`N` 范围为 `1-100`。`AnalysisRun` 按公司、`run_type`、`analyst_profile` 分组；其他版本按公司分组。计划始终保护：

- `is_latest` 分析运行和 Memo，以及当前最新估值、价格决策。
- `archived` Memo。
- 被保留的价格决策引用的估值和 Memo。
- 被保留的估值引用的 Memo。
- 被保留 Memo 的版本链父记录、生成运行和 `source_analyst_run_ids` JSON 来源。
- 被保留 AnalysisRun 的 `parent_run_id` 父版本。

执行顺序为 `PriceDecisionRun -> ValuationRun -> InvestmentMemo -> AnalysisRun`。

### 软删除清理

物理删除 `status=deleted` 的价格决策，以及没有被估值、价格决策或 Memo 版本链引用的 deleted Memo。`archived` Memo 永不作为垃圾。被引用记录在 Preview 中显示阻塞原因。删除提交后执行 integrity check、`VACUUM` 和 `ANALYZE`，并返回压缩前后大小。

## 备份与恢复

默认目录为 `data/backups/`。每份备份使用 SQLite Backup API 创建，目录包含 `database.sqlite3` 和 `manifest.json`。manifest 保存备份 ID、UTC 创建时间、文件名、大小、SHA-256、SQLite schema version、所有业务表记录数、创建原因和应用版本。

支持手动创建、列表、SHA-256/完整性校验、两阶段恢复和两阶段删除。恢复前创建 `before_restore` 备份；目标恢复失败时自动恢复该备份。恢复成功后执行 integrity check 并返回 `restart_required=true`。

自动备份设置包括启用状态、`1-720` 小时间隔和 `1-200` 份保留数量。后台线程随应用启动和关闭，等待期间不阻塞 API；自动备份也使用维护锁。

## 数据库初始化

完整初始化要求确认短语“完整初始化数据库”。执行时先备份并校验，随后关闭当前会话、重建 SQLAlchemy 表、执行当前轻量迁移、写入 61 家标准公司种子并执行 integrity check。任一步失败都会从操作前备份恢复。成功响应包含 `restart_required=true`。

## API

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `GET` | `/api/data-management/summary` | 数据库和备份概览 |
| `POST` | `/api/data-management/operations/preview` | 生成操作计划与令牌 |
| `POST` | `/api/data-management/operations/execute` | 执行令牌绑定操作 |
| `GET/POST` | `/api/data-management/backups` | 备份列表与手动创建 |
| `POST` | `/api/data-management/backups/{backup_id}/verify` | 校验备份 |
| `POST` | `/api/data-management/backups/{backup_id}/restore/preview` | 恢复预览 |
| `DELETE` | `/api/data-management/backups/{backup_id}` | 使用预览令牌删除备份 |
| `GET/PUT` | `/api/data-management/settings` | 自动备份设置 |

参数配置中心独立使用 `/api/parameter-config` API。当前生效参数存放在固定源码文件 `backend/app/configuration/active_parameters.json`，不属于 SQLite 备份；008-011 的运行参数快照仍随数据库备份和恢复。旧 `parameter_config_versions` 表为数据库兼容保留，但初始化和配置流程不再写入该表。

## 前端

页面包含数据库概览、公司清理、全局分析历史、版本保留、备份恢复和数据库维护六个区域。真实影响数量和阻塞原因均来自后端。危险操作在对话框内展示计划，并要求输入后端返回的确认短语；页面区分 loading、success、error、blocked 和 restart-required 状态。

## 关键文件

- `backend/app/api/routes/data_management.py`
- `backend/app/schemas/data_management.py`
- `backend/app/services/data_management_service.py`
- `backend/app/services/backup_service.py`
- `backend/app/services/maintenance_gate.py`
- `frontend/src/app/DataManagementView.tsx`

## 参数配置中心（2026-08-16）

配置中心是独立工作台页面，覆盖数据采样、财务预警、分析师引擎、规则矩阵、Memo 决策、估值模型和价格决策七个域。所有参数在所属域内直接展示，不再区分普通/专家模式；百分数以 UI 百分比编辑、后端小数存储；支持单项/分组恢复默认、完整校验、警告二次确认和直接发布源码参数。

编辑过程只存在于浏览器内存，不调用写接口；关闭或刷新页面即丢弃，不留草稿、版本或历史记录，也不影响运行。确认发布后，后端先执行完整结构校验，再用同目录临时文件和原子替换覆盖 `active_parameters.json`。运行期只接受通过完整校验的源码参数，否则整包回退内置默认配置并记录原因。默认配置当前包含 372 个实际计算数值；92 个早期方向审计系数不参与计算，也不在前端展示或开放编辑。

API 仅包括 `GET /current`、`GET /defaults`、`POST /validate` 和 `PUT /current`。旧草稿、版本、历史和回滚路由已移除。前端入口为“参数配置”，实现文件为 `frontend/src/app/ParameterConfigView.tsx`。

专项测试覆盖多错误一次返回、缺少源码文件时不落盘、发布原子覆盖、损坏配置整包回退、校验不落盘、警告二次确认和旧版本路由不存在。完整后端测试 `195 passed`，前端测试 `54 passed`，前端 production build 与后端 Ruff 检查均通过。

### 2026-08-16 配置修改与中文显示说明

- 配置中心不会修改 Python/TypeScript 程序文件；“发布”会直接覆盖仓库内唯一的 JSON 源码参数文件。校验不会写文件，未发布修改不会留记录或生效。
- 页面刷新始终重新读取当前源码参数；不存在源码文件时读取内置默认参数。旧运行继续使用自身的 `config_hash/config_snapshot`，不依赖配置版本。
- 前端参数名称、作用说明、默认值、风险等级、字符串选项、规则角色和 13 个实际估值维度全部使用中文显示。作用说明解释参数控制对象、增减方向及生效步骤；内部英文代码键不在配置页展示。
- `parameter_impacts` 是旧方向解释层，若与现行 13 维映射同时使用会重复计算，所以只在内部数据中兼容保留；前端已移除“参数方向审计/仅审计”整列。
- 参数名称可换行且不截断；规则矩阵在手机端改为纵向展开。Playwright 已遍历七个配置域，在 1440px 与 390px 视口验证无英文参数、无未命名项、无文字裁切和无页面级横向溢出。

### 2026-08-17 最终交互验证

- 手机端七个配置域改为自动换行；规则矩阵改为逐条纵向展示“分析师/规则、计算角色、实际计算维度”，不再依赖横向滚动。390px 视口检查结果为页面溢出 0、矩阵溢出 0、文字裁切 0。
- Playwright 将“公司列表默认数量”从 20 临时改为 21 并执行校验：发布确认正常出现，`active_parameters.json` 仍不存在；刷新后恢复为 20，证明未发布编辑不落盘、不生效。
- 运行地址为前端 `http://127.0.0.1:5173`、后端 `http://127.0.0.1:8004`。前端代理下的 current/defaults 均返回 200，旧 `/api/parameter-config/versions` 返回 404。

### 2026-08-17 参数页细节修正

- 删除普通/专家分段控件；每个配置域同时展示全部非审计参数，规则矩阵同时展示其全局参数和 40 条规则映射。
- 数值输入与默认值在显示前最多按 10 位十进制归整，整数保持整数形式；规则状态分、计数、权重指数、维度分、情景倍数等使用真实单位，不再错误显示为百分比。
- 分析师和维度筛选仅保留浏览器原生下拉箭头；实际计算维度采用紧凑的名称加输入框布局，桌面实测间距 5px。
- Playwright 在 1440px 和 390px 验证模式按钮 0、额外下拉图标 0、长浮点输入 0、页面/矩阵溢出 0、文字裁切 0。真实估值运行的审计区英文参数键和超过 4 位的小数均为 0。

### 2026-08-17 参数中文语义逐项审计

- 新增后端统一中文文案目录 `backend/app/configuration/parameter_copy.py`，前端直接使用后端返回的名称和作用说明，不再根据英文键套用同质化模板。七个域共 269 个前端可见非规则字段均有唯一中文名称和唯一说明，未命名项与占位说明均为 0。
- 每项说明按真实代码路径写明触发条件、计算公式、增减方向、单位和下游影响；111 个规则矩阵系数按具体分析师、规则和维度动态说明。价格参考规则明确隔离，92 个旧方向审计系数继续不展示、不编辑、不参与计算。
- 逐项核对时接通了此前仍被前端硬编码或业务未读取的公司列表、财务期数、公告分页、外部搜索数量、证据截断、来源优先级、Memo 列表和价格历史数量；同时纠正手工导入模型摘录与审计快照长度用反的问题。
- 校验结果：后端 `197 passed`，前端 `56 passed`，Ruff 与 production build 通过。Playwright 遍历七个域的 1440×1000 和 390×844 视口，每个域的未命名项、缺少说明、文字裁切和页面横向溢出均为 0。
- 发布语义保持不变：编辑和校验不写文件；确认发布后原子覆盖 `backend/app/configuration/active_parameters.json`，只影响后续新运行。Python/TypeScript 程序文件以及历史运行快照都不会被发布操作改写。
