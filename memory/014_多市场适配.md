# 014 多市场适配

## 模块定位

014 是横跨 002-013 的市场基础设施升级，不是 004-011 研究主链路中的第九步，也不新增独立研究结论。目标是在保持 A 股现有行为、008-010 价格盲区、历史运行不可变和 013 参数单一事实源的前提下，让港股和美股普通股能够完成 004-011 全链路研究。

首轮支持范围：

- A 股普通股：保持现有完整能力和兼容 API。
- 港交所普通股：公司与证券资料、HKD 行情、结构化财务、HKEX 披露、外部证据、008-011 全链路。
- NASDAQ、NYSE、AMEX 普通股：公司与证券资料、USD 行情、SEC 财务与披露、外部证据、008-011 全链路。
- CNY、HKD、USD 财务和交易币种，以及三者间 011 汇率换算。
- 同一发行人的多个上市证券、普通股类别和可选 ADS/ADR 换算比例。

首轮不包含 ETF、基金、期权、权证、债券、优先股、OTC、自动交易、组合管理和交易级实时行情。银行、保险等行业仍受 010 现有通用估值模型限制；多市场适配不能把“有财务数据”误写成“估值模型完全适用”。

## 当前实现审计

| 模块 | 已有能力 | 阻碍港美股完整闭环的问题 |
| --- | --- | --- |
| 002 | Windows 本地开发、FastAPI、React、SQLite、模型与搜索配置 | 没有 SEC User-Agent、AKShare 和多市场数据源配置 |
| 003 | schema v4 代码迁移、`Company` 作为全部研究根 | 文档仍写 v3；发行人与上市证券没有分层；维护服务只认识现有 8 张业务表 |
| 004 | A/H/美股种子和行情代码；`.SH/.SZ/.HK/.US` 行情 | `Company` 同时承担发行人与证券；行情没有币种；港美股档案与分红不完整；同名证券会被原地升级 |
| 005 | A 股四类财务底稿、证据包、TTM/年度正常化 | 当前 F10 客户端直接传 `.HK/.US` 只会返回来源失败；大量展示和 008 模型单位仍假设 CNY；报告期缺少显式类型与起止日 |
| 006 | A 股公告同步、正文、摘要和 008 引用 | 公告适配器只接受 6 位 A 股代码；缺少来源文档 ID、表单类型、语言和正文抓取审计字段 |
| 007 | 通用网页搜索、手工导入；已生成港交所和 SEC 官方入口线索 | 默认 fallback 仍实例化 A 股披露提供器，部分 query 和最终官方线索硬编码上交所、巨潮及中国监管来源 |
| 008 | 8 个 Profile、32 条规则、五状态、临时联网搜索、严格价格盲区 | 财务展示单位和公司上下文尚未形成动态币种/市场合同；临时搜索缺少市场化官方源提示 |
| 009 | 纯叙事 Memo，至少 2 个成功 008 来源，严格价格盲区 | 数值逻辑无需市场化；只需继承正确发行人、来源与币种上下文 |
| 010 | 8 人完整门禁、117 个系数、动态安全边际、五模型估值 | 估值结果和方法单位硬编码 `CNY`；每股价值没有明确普通股基准；字符串报告期解析不足以覆盖不同财政年度 |
| 011 | 冻结 010 和 Memo 后读取当前价 | memory 描述 `PriceSnapshot`，但当前 ORM 和服务仍直接读取 `Company.current_price`；没有上市证券、交易币种、汇率或 ADS 比例快照 |
| 012 | 两阶段清理、备份、恢复、版本保留和 VACUUM | 新增上市证券、行情和汇率表后，统计、备份、清理和引用保护都需更新 |
| 013 | 8 个配置域、375 个活动数值、完整快照和强校验 | 尚无市场数据域；数据源凭据与用户可调参数的边界未定义 |

当前代码与文档存在三项需要在编码前解决的基线漂移：

1. `backend/app/db/migrations.py` 已是 SQLite schema v4，`memory/003` 仍写 v3。
2. `memory/011` 描述独立 `PriceSnapshot`，当前模型、schema 和服务不存在该实体。
3. `memory/001` 的早期 10 Profile/40 规则描述已经过时；当前硬合同是 8 Profile、32 规则、13 维、117 系数和五状态。

## 核心架构决策

### 保留 Company 作为发行人级研究根

不整体迁移现有 `company_id` 外键。`Company` 继续承载发行人级研究关系，财务、公告、Evidence、008、Memo 和估值仍按 `company_id` 归属。新增 `SecurityListing` 表承载上市证券身份。

为兼容当前 API 和数据库，`Company.ticker/exchange/listed_date` 及现有行情字段暂时保留，作为主上市证券的兼容镜像；新业务的证券事实源必须是 `SecurityListing` 和 `MarketSnapshot`。所有修改主上市证券的服务都要原子更新兼容镜像，禁止前后两套值各自演化。

该方案避免一次性重写全部研究外键，也终止现有“发现同名 A 股后原地覆盖港股身份”的策略。种子和迁移以后使用稳定 `canonical_key` 识别发行人，同一发行人可以有多个 Listing。

### 分离发行人币种、交易币种和估值币种

- `Company.reporting_currency`：发行人财务报表及 010 总股权价值的币种。
- `SecurityListing.trading_currency`：目标上市证券的报价币种。
- `FinancialStatement.currency`：单条底稿币种，构建同一证据包时必须一致。
- `ValuationRun.valuation_currency`：确认运行冻结的估值币种，来自财务口径，不从行情推断。
- `MarketSnapshot.currency`：行情价格币种，必须与 Listing 交易币种一致。

010 只在 reporting/valuation currency 中计算，不请求行情或汇率。011 才允许读取目标 Listing、MarketSnapshot、ADS/股类换算比例和 FxRateSnapshot。

### 每股基准与证券换算

010 的每股价值必须明确为“发行人估值普通股单位”的每股价值。Listing 保存：

```text
underlying_shares_per_listing_unit
```

普通股默认 `1.0`；ADS/ADR、不同股类或特殊换股单位必须提供经过来源复核的正数比例，否则 011 阻止生成，不猜测。

011 的换算公式：

```text
listing_intrinsic_value =
  issuer_intrinsic_value_per_share
  × underlying_shares_per_listing_unit
  × fx_rate(valuation_currency -> trading_currency)

buy_price = listing_intrinsic_value × (1 - effective_safety_margin)
```

同币种时汇率固定为 1 且不请求外部来源。跨币种时冻结汇率数值、日期、来源、币种方向和公式版本。

### 数据源使用适配器注册表

服务层不得继续直接实例化某一个东方财富客户端。新增市场上下文和能力注册表：

```text
MarketContext
ProviderCapabilities
IssuerProfileProvider
MarketSnapshotProvider
FinancialStatementProvider
DisclosureProvider
FxRateProvider
```

统一错误类型至少区分：unsupported、not_configured、rate_limited、upstream_failed、parse_failed、validation_failed。前端根据能力状态显示 available、partial、unavailable、stale、blocked 及中文原因，不能让用户点击后才发现整个市场不支持。

## 数据模型

### Company 增补字段

- `canonical_key`：稳定发行人键，种子和跨上市地合并使用。
- `legal_name`：法定名称。
- `aliases`：中英文名、历史名和常用名。
- `domicile_country`：ISO 国家或地区代码。
- `reporting_currency`：ISO 4217 币种。
- `fiscal_year_end`：财政年度截止月日。
- `external_ids`：如 SEC CIK、LEI 等发行人级标识。

### SecurityListing

- `id`、`company_id`。
- `ticker`、`symbol`、`exchange`、`market`。
- `trading_currency`、`security_type`。
- `listed_date`、`is_primary`、`is_active`。
- `underlying_shares_per_listing_unit`。
- `provider_identifiers`：SEC CIK/ticker、HKEX stock ID、东方财富 secid 等证券或来源标识。
- `created_at`、`updated_at`。

约束：`exchange + ticker` 唯一；同一公司最多一个 active primary listing。首轮只允许 `security_type=common_stock/ads` 进入完整链路。

### MarketSnapshot

行情刷新每次创建不可变快照，同时更新 Company 兼容镜像：

- `listing_id`、`price`、`currency`、`market_cap`。
- `pe_ttm`、`pe_dynamic`、`pe_static`、`pb_ratio`、`ps_ratio`。
- `dividend_yield_ttm`、`dividend_yield_static`。
- `price_as_of`、`fetched_at`、`source`、`source_url`。
- `raw_snapshot_hash`，可选受限原始快照。

011 必须绑定 `market_snapshot_id`，不能只复制当前 Company 值。历史 `PriceDecisionRun` 允许该外键为空，并继续使用自身已冻结价格，不伪造来源。

### FxRateSnapshot

- `base_currency`、`quote_currency`、`rate`。
- `rate_date`、`fetched_at`、`source`、`source_url`。
- `raw_snapshot_hash`。

同一日期、币种方向和来源去重。ECB 以 EUR 为基准时，交叉汇率按 `quote_per_eur / base_per_eur` 计算并保存计算审计。

### FinancialStatement 增补字段

- `source_record_id`、`filing_type`、`taxonomy`。
- `period_start`、`period_end`、`period_type`。
- `fiscal_year`、`fiscal_period`、`filed_at`。
- `unit_scale`、`is_amendment`、`raw_snapshot_hash`。

保留现有 `period` 展示字段和 `(company_id, period, statement_type)` upsert 语义。一个 Company 同一时间只选择一个活动财务来源，禁止把 HKEX/SEC/东方财富不同币种记录混在同一个证据包。来源切换必须先预览覆盖影响并显式确认。

### Announcement 增补字段

- `listing_id` 可空，用于标识披露所对应的上市证券。
- `source_document_id`、`document_type`、`filing_form`。
- `language`、`period_end`、`content_type`。
- `content_source`、`content_fetched_at`、`raw_content_hash`。

去重优先使用 `source + source_document_id`，其次使用规范化 URL，最后才使用标题与发布时间。同步清理必须按来源和 Listing 分区，不能让一个市场的同步删除另一个市场的披露。

### ValuationRun 与 PriceDecisionRun

- `ValuationRun` 增加 `valuation_currency` 和 `share_basis_snapshot`。
- `PriceDecisionRun` 增加 `listing_id`、`market_snapshot_id`、可空 `fx_rate_snapshot_id`。
- 价格决策冻结 `valuation_currency`、`trading_currency`、`underlying_shares_per_listing_unit`、汇率和换算前后内在价值。
- 新记录不再写入旧分析师评分兼容字段；旧记录原样保留。

## 数据源方案

| 能力 | A 股 | 港股 | 美股 |
| --- | --- | --- | --- |
| 发行人与证券档案 | 现有东方财富 | AKShare 东方财富港股证券/公司资料；HKEX 证券列表复核 | SEC submissions 的名称、历史名、ticker、exchange、SIC、fiscal year end |
| 行情与估值倍数 | 现有东方财富 | 现有东方财富行情，币种由 Listing 明确为 HKD 或实际柜台币种 | 现有东方财富行情，币种由 Listing 明确为 USD |
| 结构化财务 | 现有东方财富 F10 | AKShare `stock_financial_hk_report_em`、指标接口；HKEX 年报/业绩公告作审计来源 | SEC `submissions` + `companyfacts`，支持 us-gaap 与 ifrs-full 标准概念 |
| 官方披露 | 现有东方财富公告 | HKEXnews Listed Company Information Title Search 及原始 PDF | SEC submissions、Archives 主文档及 XBRL 文件 |
| 分红 | 现有 BonusFinancing | AKShare 港股分红接口和 HKEX 权益披露复核 | SEC XBRL 现金分红事实；缺失时明确数据缺口 |
| 外部证据 | DuckDuckGo/Bing + A 股官方 fallback | HKEX、SFC、发行人 IR、香港政府公开数据的市场化 query | SEC、发行人 IR、BEA/BLS/Census 及行业监管机构的市场化 query |
| 汇率 | 同币种不需要 | ECB CNY/HKD 或其他 reporting currency/HKD 日参考汇率 | ECB CNY/USD、HKD/USD；USD/USD 为 1 |

数据源边界：

- SEC JSON API无需 API Key，但后端必须配置明确的 `SEC_USER_AGENT`，集中限速到不高于官方当前 10 请求/秒，并实现缓存、重试和 429/403 诊断。
- HKEXnews 是官方披露事实源，但当前公开资料没有与 SEC Company Facts 等价的统一结构化财务 API。本方案因此把港股结构化财务适配器隔离在 Provider 后，并用 HKEX 原始披露做来源审计；不把页面内部未公开接口当稳定合同。
- AKShare 需要固定版本，并把 pandas DataFrame 立即转换为项目 DTO；领域服务和测试不得依赖中文列名或 pandas 对象。
- ECB 汇率是信息用途日参考率，不是成交汇率。011 必须展示来源日期，不得表述为交易执行价格。
- 标准自动测试只使用固定 fixture，不调用真实外网；另设显式 smoke 测试验证真实数据源。

## 002-013 逐模块改造

### 002 环境与依赖

- 增加并固定 AKShare 版本及其必要依赖。
- 增加 `SEC_USER_AGENT` 示例配置，格式包含应用名和联系邮箱。
- 增加数据源超时、缓存目录和显式 smoke test 命令。
- 真实外网 smoke test 不进入默认 pytest。

### 003 数据库与迁移

- 从当前实际 v4 迁移到新版本，先创建新表，再回填每个 Company 的主 Listing。
- 保留所有 Company ID 和 004-011 历史关系。
- 历史 Company 行情回填为一条 legacy MarketSnapshot；无法确定币种时根据种子 Listing 明确映射，非标准证券不猜测。
- 旧 PriceDecisionRun 不伪造 market/fx snapshot 外键。
- 种子改为发行人 + Listings；中国移动和阿里巴巴可增加跨市场 Listing，但不能覆盖原 Listing。
- 更新迁移幂等、连续升级和外键完整性测试。

### 004 公司档案与行情

- 搜索同时匹配发行人名称、aliases、Listing ticker、exchange 和 market。
- 公司详情返回主 Listing 摘要；另提供 Listings 和 capability API。
- 保留旧 `/profile/refresh` 作为主 Listing 兼容入口，新增 Listing 级档案/行情刷新。
- 前端公司标题展示发行人名称、当前 Listing、市场和交易币种；多 Listing 时使用选择菜单。
- 刷新行情创建 MarketSnapshot，并显示数据时间、来源和币种。

### 005 财务底稿

- Provider 输出统一 DTO，不让 SEC tag、AKShare 中文列名或东方财富字段进入服务层。
- 建立 US-GAAP、IFRS/HKFRS 到现有标准字段的显式映射表，保留原始 taxonomy/tag/source_record_id。
- 同一标准字段出现多个候选 tag 时按可审计优先级选择，并把冲突和回退写入数据质量诊断。
- 使用 `period_start/end/type` 取代字符串猜测作为正常化事实源；`period` 只负责展示兼容。
- `financial_evidence_pack` 增加 reporting_currency、accounting_standard、source_coverage 和 mapping_diagnostics。
- 把 `latest_amounts_100m_cny` 等 CNY 专用模型展示改为动态币种显示，同时对旧 CNY 快照保持读取兼容。

### 006 披露与摘要

- 把现有 A 股公告客户端纳入 DisclosureProvider。
- HKEX 同步年报、中期报告、业绩公告、重大事项、监管与治理披露；保留中英文和 PDF 来源。
- SEC 同步 10-K、10-Q、8-K、20-F、6-K、DEF 14A 及 amendments；分类映射到内部 document_type。
- SEC HTML/inline XBRL 优先读取正文；HKEX PDF 先做文本提取，扫描件形成 OCR/人工复核缺口，不能伪造正文。
- 摘要输出合同保持不含价格、目标价或交易动作；008 仍按公告 ID 引用。

### 007 外部证据

- 查询规划接收 MarketContext、发行人 aliases 和全部 Listing 标识。
- fallback 按市场路由，移除对港美股继续生成 SSE/CNInfo 线索的行为。
- `KnownOfficialDisclosureSearchProvider` 已有 HKEX/SEC 入口，应纳入真实 fallback 链，而不只生成静态线索。
- 港美股优先英文原始来源，最终 Evidence 仍输出中文结构化摘要并保留原始标题、语言和 URL。
- 不在 014 顺便修改 Evidence 复核、分页和跨运行去重等既有独立债务，除非它们直接阻断多市场链路。

### 008 分析师视角

- 8 Profile、32 规则、五状态、117 系数和价格硬门禁完全不变。
- 输入加入发行人、会计准则、报告币种和非价格 Listing 身份；不得加入行情、MarketSnapshot 或 FxRateSnapshot。
- 金额展示按实际币种生成，禁止把 HKD/USD 标成亿元 CNY。
- 临时 `web_search` 工具说明加入市场官方源提示，但仍不将临时结果写入 Evidence 或伪造来源 ID。

### 009 投资备忘录

- 继续只做叙事和来源归并，不恢复评分卡或安全边际。
- Memo 来源图保留 market、language、accounting_standard 和 currency 上下文。
- 价格盲区与至少 2 个成功来源门槛不变。

### 010 无锚定估值

- 8 人最新成功、每人 4 规则和有效 Memo 门禁不变。
- 移除所有硬编码 CNY，输出币种取自一致财务证据包并冻结为 valuation_currency。
- 使用显式 period_type/start/end/fiscal period 做 TTM 和年度正常化，兼容自然年、非自然财政年、半年报和 52/53 周财政年。
- `shares_outstanding` 必须带 share basis 和来源。存在多股类但无法确定估值普通股单位时，只允许总股权价值，阻止每股价值和 011。
- 五模型、参数矩阵、动态安全边际和价格盲区公式不因市场改变。

### 011 价格对照与投资决策

- 创建请求增加可选 `listing_id`，省略时使用主 Listing。
- 读取 Listing 最新可用 MarketSnapshot；过期时要求刷新，不能静默使用 Company 兼容价格。
- 先按 share ratio 转为 Listing 单位，再按冻结 FX 转为交易币种，最后计算安全边际与价格状态。
- 响应展示 valuation currency、trading currency、FX date/rate/source、Listing ratio、换算前后内在价值。
- 011 仍不重算 008、Memo 或 010 动态安全边际。

### 012 数据管理

- 备份 manifest、summary 和初始化纳入 SecurityListing、MarketSnapshot、FxRateSnapshot。
- 单公司研究数据重置保留 Company、Listings 和最新市场资料；删除其财务、披露、Evidence 及 008-011 派生数据。
- 版本保留必须保护 PriceDecision 引用的 MarketSnapshot 和 FxRateSnapshot。
- 增加行情/汇率快照保留策略：始终保护 latest 和被 PriceDecision 引用记录，再按 Listing/币种对保留最近 N 条。
- 初始化后写入发行人和 Listing 种子，不再执行跨市场身份覆盖。

### 013 参数配置

- 新增 `market_data` 配置域，只存用户可调运行参数，例如行情最大时效、汇率最大时效、各市场披露回看期和快照保留数量。
- SEC User-Agent、Provider URL、缓存路径和凭据继续放环境变量或代码级结构配置，不进入前端参数页。
- 更新完整配置校验、中文元数据、活动数值计数和发布测试；不能手工保留旧 375 计数。
- 新参数只影响后续同步和运行，历史快照不追溯修改。

## API 与前端建议

新增或演进的核心 API：

```text
GET  /api/companies/{company_id}/listings
POST /api/companies/{company_id}/listings
GET  /api/companies/{company_id}/market-capabilities
POST /api/listings/{listing_id}/profile/refresh
POST /api/listings/{listing_id}/market-snapshots/refresh
GET  /api/listings/{listing_id}/market-snapshots/latest
POST /api/companies/{company_id}/financials/sync?listing_id=...
POST /api/companies/{company_id}/announcements/sync?listing_id=...
POST /api/companies/{company_id}/price-decision-runs
```

旧 Company、财务、公告和价格决策 API 尽量保留；未传 Listing 时使用主 Listing。新响应采用嵌套 `primary_listing`、`capabilities` 和币种审计字段，旧顶层 ticker/price 字段在兼容期继续返回。

前端不增加“014”导航。改造现有页面：

- 公司搜索结果显示市场和交易币种；同一发行人展开多个 Listing。
- 公司工作台标题提供 Listing 选择菜单，不用多个重复 Company 模拟多地上市。
- 财务页显示报告币种、会计准则、表单和期间类型。
- 公告页显示市场、表单/文档类型、语言和原始来源。
- 估值页显示 valuation currency 和 share basis，但不显示行情或 FX。
- 价格决策页显示目标 Listing、交易币种、汇率与证券单位换算审计。
- capability 不可用时禁用对应同步按钮并给出具体修复项。
- 桌面和 390px 手机视口均不得出现横向页面溢出、标签重叠或按钮文字裁切。

## 实施阶段

### Phase 0：基线冻结

- 运行后端 Ruff/pytest、前端 test/build，记录真实基线。
- 修正 001、003、011、012 等与当前代码不一致的文档事实。
- 为当前 A 股行为补齐缺失回归，尤其是 schema v4、011 当前价格冻结和 8/32/117 合同。

### Phase 1：市场基础层与 A 股回归

- 新增 Company 字段、SecurityListing、MarketSnapshot、FxRateSnapshot 和迁移。
- 回填全部现有 Company 的主 Listing，保持 Company ID。
- 建立 Provider 协议、注册表、能力 API和统一错误。
- 把现有 A 股 Profile、行情、财务、公告迁入注册表，确保已有测试和页面行为不退化。

### Phase 2：美股垂直闭环

- SEC ticker/CIK、submissions、companyfacts 和 filing 文档适配。
- Apple `AAPL.US` 完成 004-011 fixture 与真实 smoke test。
- 验证非自然财政年、10-K/10-Q、amendment、tag 回退和 USD 每股价值。

### Phase 3：港股垂直闭环

- AKShare 港股档案、财务、指标与分红适配；HKEX 披露和 PDF 正文适配。
- 腾讯 `00700.HK` 完成 004-011 fixture 与真实 smoke test。
- 验证 CNY 财务估值转 HKD 上市价格、半年报 TTM、中文/英文披露和 FX 过期门禁。

### Phase 4：多地上市和复杂证券单位

- 阿里巴巴或中国移动建立同一 Company 多 Listing 测试。
- 验证主 Listing 切换不迁移研究历史，价格决策按 Listing 分版本。
- 验证 ADS/ADR 比例缺失时阻止 011、比例存在时换算正确。
- 验证 BRK.B、GOOGL 等多股类只在 share basis 可解释时生成每股价值。

### Phase 5：运维、参数与前端收口

- 完成 012 新表统计、备份、初始化、清理和引用保护。
- 完成 013 market_data 域、中文说明、计数和快照。
- 完成前端 capability、Listing 选择、币种/FX 审计和响应式 QA。
- 同步 001-014、README 和 API 文档，只保留当前事实与明确未实现边界。

## 测试与验收

必须使用离线 fixture 覆盖：

- v4 数据库迁移后 Company ID、历史 008-011 和外键完整保留；迁移重复执行幂等。
- A 股贵州茅台完整回归，不因 Provider 抽象改变现有计算结果。
- 港股腾讯从 Profile、HKD 行情、财务、HKEX 披露到 011 的完整链路。
- 美股 Apple 从 SEC Profile、Company Facts、filings、USD 行情到 011 的完整链路。
- 阿里巴巴或合成发行人的双 Listing、主 Listing、ADS 比例和跨币种换算。
- SEC tag 多候选、amendment、非自然财政年；HK 半年报和中英文重复披露。
- 混合财务币种、未知 share basis、过期行情、过期 FX、来源限流和正文解析失败均明确阻断或降级。
- 008-010 快照中不存在 current price、market cap、MarketSnapshot、FX rate、目标价或交易动作。
- 010 的 8 Profile、32 规则、13 维、117 系数、五状态和动态安全边际端点保持不变。
- 011 只读取冻结 010 安全边际，换算前后公式和来源可复现。
- 012 不会删除被历史 PriceDecision 引用的行情或 FX 快照。
- 013 发布后只影响新同步和新运行，旧历史继续读取自身快照。
- 后端 Ruff/pytest、前端 test/build 全通过；桌面与手机浏览器 smoke test 通过。

完成定义不是“能刷新港美股价格”，而是上述港股与美股样例均可从 004 走到 011，所有币种、来源、证券单位、配置和历史快照都可追溯，同时 A 股无回归。

## 主要风险与控制

- **港股结构化财务来源变化**：AKShare 固定版本、Provider 隔离、列映射 fixture、HKEX 原文链接和缺口诊断。
- **SEC taxonomy 扩展和 tag 差异**：只直接采用标准 taxonomy，维护显式候选表，扩展 tag 不自动映射为核心事实。
- **多股类/ADR 口径错误**：比例缺失即阻断每股比较，禁止按 ticker 猜测。
- **跨币种误比较**：010 输出币种和 011 交易币种都为必填，换算快照冻结且展示方向。
- **历史兼容**：新外键对历史可空，旧 JSON 原样保留，不批量重算旧研究。
- **数据源限流**：后端集中缓存、限速、重试；默认测试不打真实外网。
- **一次改造面过大**：每个 Phase 都必须保持主分支可运行，先完成 A 股适配器迁移，再做 US/HK 垂直闭环。

## 官方与实现参考

- SEC EDGAR APIs：`https://www.sec.gov/search-filings/edgar-application-programming-interfaces`
- SEC 自动访问规则：`https://www.sec.gov/search-filings/edgar-search-assistance/accessing-edgar-data`
- HKEXnews 上市公司资料检索：`https://www.hkexnews.hk/homelcicontentsearch.html`
- HKEX Securities Lists：`https://www.hkex.com.hk/Services/Trading/Securities/Securities-Lists`
- ECB Data API 示例：`https://data.ecb.europa.eu/help/api/data-examples`
- AKShare 港股与美股数据文档：`https://akshare.akfamily.xyz/data/stock/stock.html`

## Goal 模式 Prompt

```text
目标：在 D:\study\Python.Project\Value_Investment 完整实现 014 多市场适配，使 A 股保持兼容，并让港股与美股普通股能够完成 004-011 全链路研究。

开始前：
1. 阅读根目录 AGENTS.md，以及 memory/001-014 的全部当前文档。
2. 对照当前代码确认真实基线；当前核心合同是 8 个 Profile、32 条规则、五状态、13 个维度、117 个系数，动态安全边际属于 010。
3. 先运行并记录后端 Ruff/pytest、前端 test/build。不要使用 Git，除非我明确要求。
4. 发现 memory 与代码不一致时，以代码和测试确认当前事实，先同步对应 memory，再继续实现；不能沿用过时的 10 人/40 规则、009 评分或虚构 PriceSnapshot。

不可破坏的边界：
- Company 继续作为发行人级研究根，保留所有现有 company_id 和历史关系。
- 新增 SecurityListing 表承载多地上市；Company 旧 ticker/exchange/行情字段仅作主 Listing 兼容镜像。
- 008-010 必须继续 price-blind，MarketSnapshot、FX、当前价、市值、估值倍数和交易动作不得进入其输入、输出或 UI。
- 009 只做叙事，不恢复评分卡或安全边际；010 独占分析师数值聚合和动态安全边际；011 只读取冻结 010。
- 历史运行、Memo、估值和价格决策不可原地重算；新字段对旧历史允许为空，不伪造来源或 0 值。
- 缺失币种、share basis、ADS/ADR 比例、行情或汇率时必须明确阻断，不能猜测。
- 首轮只支持 A 股、港交所普通股及 NASDAQ/NYSE/AMEX 普通股；ETF、基金、衍生品、OTC、自动交易和组合管理不在本目标。

按以下阶段持续执行，不要只停在方案或脚手架：

Phase 0 基线：
- 修正 001/003/011/012 等与代码不一致的当前事实。
- 为 schema v4、A 股现有行为、011 价格冻结和 8/32/117 合同补齐回归。

Phase 1 市场基础层：
- 为 Company 增加 canonical_key、aliases、domicile_country、reporting_currency、fiscal_year_end、external_ids。
- 新增 SecurityListing、MarketSnapshot、FxRateSnapshot；迁移并回填每个 Company 的主 Listing，保持 ID 和外键。
- MarketSnapshot 不可变；011 最终必须绑定 snapshot。历史 PriceDecision 不伪造 snapshot 外键。
- 建立 IssuerProfileProvider、MarketSnapshotProvider、FinancialStatementProvider、DisclosureProvider、FxRateProvider、ProviderCapabilities 和市场注册表。
- 把现有 A 股适配器迁入注册表并保持所有 A 股行为。

Phase 2 美股闭环：
- 使用 SEC submissions + companyfacts + Archives；配置 SEC_USER_AGENT，集中限速不高于 10 req/s，缓存、重试并处理 403/429。
- 映射 us-gaap/ifrs-full 标准概念，保留 taxonomy/tag/accession/form/period/filed_at 和映射诊断。
- 同步 10-K、10-Q、8-K、20-F、6-K、DEF 14A 及 amendment，优先读取 HTML/inline XBRL 正文。
- 让 Apple AAPL.US 使用离线 fixture 和显式真实 smoke test 完成 004-011。

Phase 3 港股闭环：
- 固定 AKShare 版本，通过隔离 Provider 使用港股证券资料、公司资料、三表、指标和分红接口；DataFrame 在适配器内转换为项目 DTO。
- 使用 HKEXnews 作为官方披露来源，支持年报、中报、业绩公告、重大事项、监管与治理文件；保留语言、文档 ID、PDF 和正文提取审计。
- 让腾讯 00700.HK 使用离线 fixture 和显式真实 smoke test 完成 004-011，包括 CNY 报表估值转 HKD 交易价格。

Phase 4 下游与多 Listing：
- 005 使用显式 period_start/end/type、fiscal year/period、reporting currency 和 accounting standard；同一证据包禁止混合币种。
- 007 按市场选择 HKEX/SFC/SEC/发行人 IR/政府公开数据，不向港美股生成 SSE/CNInfo fallback。
- 008 金额展示动态使用真实币种，但 8 人/32 规则及价格门禁不变。
- 009 仅增加市场、语言、会计准则和币种来源上下文。
- 010 移除 CNY 硬编码，冻结 valuation_currency 和 share_basis；多股类口径不清时阻止每股价值。
- 011 请求可选 listing_id，冻结 MarketSnapshot、可选 FxRateSnapshot 和 underlying_shares_per_listing_unit；按 014 文档公式换算后再计算买入价和价格状态。
- 用阿里巴巴、中国移动或合成 fixture 验证同一 Company 多 Listing、主 Listing 切换、ADS 比例和跨币种历史不可变。

Phase 5 运维、配置和前端：
- 012 纳入新表统计、备份、恢复、初始化、公司重置、快照保留和引用保护。
- 013 新增 market_data 域及完整中文元数据和强校验；SEC User-Agent、Provider URL、凭据和缓存路径仍放环境配置。更新真实活动参数计数，不保留旧 375 假值。
- 公司搜索和工作台支持 Listing 选择、能力状态、动态币种、披露语言/表单、估值 share basis 和 011 FX 审计；不新增 014 侧边栏入口。
- 保持当前中文界面风格，桌面和 390px 手机均无页面横向溢出、文字重叠或裁切。

验收要求：
- v4 迁移幂等，Company ID、历史 008-011 和外键完整保留。
- 贵州茅台 A 股完整回归；Apple 和腾讯分别完成 004-011；双 Listing/ADS/FX fixture 完成。
- 标准测试只使用固定 fixture，不访问真实外网；真实来源检查使用独立 smoke 命令。
- 覆盖非自然财政年、HK 半年报、SEC amendment/tag 回退、多语言披露、混合币种、过期行情/FX、缺失 share ratio 和数据源限流。
- 008-010 价格隔离测试继续通过；8/32/13/117、五状态及动态安全边际端点不变。
- Ruff、全部后端 pytest、全部前端测试和 production build 通过。
- 使用浏览器实际检查 A/H/美三家公司桌面和手机流程。
- 每完成一个模块立即更新对应 memory/002-014、README/API 文档；文档只写当前有效实现和明确边界，不写虚构完成状态。

执行方式：
- 按现有代码风格小步实现，每个 Phase 完成后运行相关测试，再进入下一阶段。
- 不要因为任务大而只交付抽象接口；必须完成港股、美股垂直链路和最终验收。
- 遇到第三方来源不可用时，先通过 Provider fallback、fixture 和明确 capability 降级解决；只有确实无法继续且需要我的凭据或业务决定时再提问。
- 不做与 014 无关的重构，不实现“其他功能”工具箱。
```
