# 005_公司详情财务

## 当前边界

`005` 当前只负责公司详情页中的财务数据能力。公告列表、公告原文、公告摘要和公告入库放到 `006`，不要在 `005` 中继续扩展公告逻辑。

目标是让用户在公司工作台中直接搜索、查看和管理单家公司近年财务指标，不要求用户手工阅读财报并整理 CSV。

## 当前能力

- 公司详情页展示公司基础资料和财务数据区块。
- 财务区块默认读取最近 `60` 条财务记录，用于覆盖约 `15` 年混合披露数据；如果数据不足 `60` 条，则显示全部已有记录。
- 用户点击前端按钮 `搜索财务数据` 后，后端从外部数据源拉取财务指标并入库。
- 重复搜索按稳定键更新已有记录，不生成重复条目。
- 用户可以删除单条本地财务记录。
- 所有页面展示时间固定使用东八区 `Asia/Shanghai`。
- 公司基础资料中的 `listed_date` 为空时，详情接口会尝试从外部数据源自动补齐；数据源不支持或请求失败时，不阻塞详情页加载。
- 财务列表读取时优先按 `fields.report_date` 倒序排列，缺失报告日期时再按 `period` 和 `id` 兜底，避免中文期间字符串把“中报”排到“三季报”前面。
- 财务记录接口中的 `created_at` 按东八区序列化；SQLite 读出的无时区财务创建时间按 UTC 解释后转为 `Asia/Shanghai`。
- 前端财务字段按研究阅读顺序展示：报告日期、报告类型、披露日期、收入/同比、利润/同比、每股指标、ROE、利润率、负债率、现金流质量、周转效率和原始来源辅助字段；未登记字段按字段名兜底排序。

## 后端接口

- `GET /api/companies/{company_id}/financials?limit=60&offset=0`
  - 只读取本地数据库，不主动访问外部数据源。
  - 返回 `items/total/limit/offset`。
- `POST /api/companies/{company_id}/financials/sync?limit=60`
  - 从外部数据源搜索并同步财务数据。
  - 默认和最大同步条数均为 `60`。
  - 返回同步统计，包括已搜索、创建、更新数量。
- `DELETE /api/companies/{company_id}/financials/{statement_id}`
  - 删除本地数据库中的单条财务记录。
  - 删除前校验公司存在、记录存在且记录属于该公司。
  - 删除结果返回 `{"id": statement_id, "deleted": true}`。
- `GET /api/companies/{company_id}`
  - 读取公司基础资料。
  - 当 `listed_date` 缺失时，尝试通过东方财富公司概况补齐并保存。

## 数据源

当前 MVP 使用东方财富 F10，不依赖 AkShare。

- 财务指标来源：
  - 东方财富 F10 主要财务指标。
  - 数据集：`RPT_F10_FINANCE_MAINFINADATA`。
  - 应用标识：`APP_F10_MAINFINADATA`。
  - 请求来源：`source=HSF10`。
  - 本地记录来源名：`eastmoney_f10_main_finance`。
- 公司上市日期来源：
  - 东方财富公司概况 `CompanySurvey/PageAjax`。
  - 读取字段：`LISTING_DATE`。
- 股票代码格式：
  - A 股使用类似 `600519.SH`、`000001.SZ` 的 `ticker`。

## 入库规则

财务数据存入 `financial_statements` 表。

- 稳定 upsert key：`company_id + period + statement_type`。
- 当前 `statement_type`：`main_financial_indicators`。
- 标准化指标写入 `fields` JSON。
- 保存 `source` 和 `source_url`，用于回溯外部来源。
- 删除按钮只删除本地记录；如果再次点击 `搜索财务数据`，外部数据源仍返回该期间数据时会重新入库。

## 字段映射

- `TOTALOPERATEREVE` -> `revenue`
- `MLR` -> `gross_profit`
- `PARENTNETPROFIT` -> `net_profit`
- `KCFJCXSYJLR` -> `deducted_net_profit`
- `EPSJB` -> `eps`
- `BPS` -> `bps`
- `ROEJQ` -> `roe`
- `XSMLL` -> `gross_margin`
- `XSJLL` -> `net_margin`
- `ZCFZL` -> `asset_liability_ratio`
- `TOTALOPERATEREVETZ` -> `revenue_yoy`
- `PARENTNETPROFITTZ` -> `net_profit_yoy`
- `MGJYXJJE` -> `operating_cash_flow_per_share`
- `JYXJLYYSR` -> `operating_cash_flow_to_revenue`
- `TOAZZL` -> `total_assets_turnover`
- `CHZZTS` -> `inventory_turnover_days`
- `REPORT_DATE`、`report_type`、`NOTICE_DATE`、`raw_secucode`、`raw_security_name` 保留为辅助字段。

比例归一化规则：

- 东方财富返回的百分数字段统一转成内部比例值，例如 `32.53` 入库为 `0.3253`，前端再显示为 `32.5%`。
- 当前按比例归一化的字段包括 `ROEJQ`、`XSMLL`、`XSJLL`、`ZCFZL`、`TOTALOPERATEREVETZ`、`PARENTNETPROFITTZ`、`JYXJLYYSR`。

## 前端位置

主要位置：`frontend/src/app/CompanyWorkspaceView.tsx`

- 常量 `FINANCIAL_STATEMENT_LIST_LIMIT = 60` 控制默认读取和同步条数。
- 常量 `DISPLAY_TIME_ZONE = "Asia/Shanghai"` 控制展示时间。
- 财务区块提供：
  - 财务数据列表。
  - 核心财务字段按固定研究顺序展示，金额字段自动以万/亿缩写，比例字段显示为百分比。
  - `搜索财务数据` 按钮。
  - 单条财务记录删除按钮。
  - 同步和删除过程中的加载、禁用和错误提示。

API client 位置：`frontend/src/services/api.ts`

- `getCompanyFinancials`
- `syncCompanyFinancials`
- `deleteCompanyFinancialStatement`

## 后端位置

- 路由：`backend/app/api/routes/companies.py`
- 业务逻辑：`backend/app/services/companies.py`
- 财务数据源：`backend/app/data_sources/eastmoney_financials.py`
- 公司概况数据源：`backend/app/data_sources/eastmoney_company_profile.py`
- schema：`backend/app/schemas/company.py`

## 当前限制

- 当前主要支持东方财富可返回的 A 股主要财务指标。
- 不支持的市场、无效股票代码或外部数据源失败，应在前端显示错误原因，不要求用户手工整理 CSV。
- 当前删除是本地删除，不是永久忽略。若后续需要“删除后搜索也不再恢复”，应新增 ignored/suppressed 记录表。
- `005` 不负责公告数据，不负责财报 PDF 解析，不负责模型总结或投资判断。

## 后续财务指标利用方案

根据 `001_价值投资分析师系统架构.md`，财务数据应从“指标列表”升级为可复用的财务证据包：

- 005 继续负责财务数据入库、字段标准化和确定性指标计算，不让模型现场猜公式。
- 建议后续补齐三张表关键字段：利润表、资产负债表、现金流量表、分红回购和股本变化。
- 财务证据包应同时保留三层：`financial_facts` 绝对金额事实、`financial_metrics` 派生比率指标、`financial_trends` 多期趋势指标。绝对金额用于判断体量和估值基数，比率用于判断质量，趋势用于判断持续性。
- `financial_facts` 建议覆盖：营业收入、毛利、归母净利润、扣非净利润、经营现金流、自由现金流、总资产、股东权益、货币资金、有息负债、分红、回购和股本。
- `financial_metrics` 建议覆盖：ROE、ROIC、毛利率、净利率、经营现金流/净利润、自由现金流/净利润、资产负债率、现金/有息负债、分红率、回购率和股本稀释率。
- `financial_trends` 建议覆盖：收入 3/5 年 CAGR、净利润 3/5 年 CAGR、自由现金流 3/5 年 CAGR、ROE 稳定性、毛利率稳定性、净利率稳定性、收入同比、净利润同比和单季度趋势。
- 后端派生 `financial_metrics`：盈利质量、现金质量、成长质量、资产质量、财务安全、股东回报。
- 后端派生 `financial_flags`：现金流背离、应收/存货异常、商誉压力、杠杆压力、利润率下滑、股本稀释等。
- 后端派生 `financial_data_gaps`：缺少资本开支、有息负债、应收、存货、商誉、分红或回购等会降低置信度的缺口。
- 008 分析师视角读取财务证据包，用于输出 `financial_observations`、规则检查和后续估值假设建议；不得把财务数据当作 007 外部证据。
- 010 无锚定估值实验室读取同一套财务证据包中的现金流、增长、资本回报、资本开支、分红和资产负债字段，生成保守/中性/乐观内在价值区间；此阶段禁止读取当前价格、市值、历史价格、持仓成本、评级和市场情绪。
- 011 价格对照与投资决策只能在估值锁定后读取价格和市值，计算安全边际、买入区间、卖出区间和持有状态。

阶段使用口径：

- 008 分析师阶段回答“是不是好公司、风险在哪里、哪些估值假设需要复核”。它可以使用财务绝对数、比率和趋势，但输出应停留在质量判断、风险识别和估值假设建议，不直接生成买卖建议。
- 010 无锚定估值阶段回答“公司大概值多少钱”。它应使用基准自由现金流、基准净利润、成长率、ROIC、资本开支、分红回购、现金和有息负债等基本面输入，生成保守/中性/乐观内在价值区间，并保存 `price_blind=true`。
- 011 价格对照阶段回答“当前价格下是否有安全边际以及如何行动”。只有在 010 估值锁定后，才允许读取当前价格、市值、历史估值分位和持仓成本。

结构化证据包传递方式：

- 005 生成的 `financial_evidence_pack` 应作为后端标准财务中间层复用，不替代原始 `financial_statements`。`financial_statements` 负责追溯原始期间记录，`financial_evidence_pack` 负责提供已经整理好的事实、指标、趋势、旗标和缺口。
- 008 构建 `analyst_view` 快照时，应在顶层同时传入 `financial_statements` 和 `financial_evidence_pack`。分析师优先读取 `financial_evidence_pack` 形成 `financial_observations`、`risk_flags`、`data_gaps` 和 `valuation_assumption_suggestions`，需要引用具体财务证据时仍使用 `rule_checks[].financial_periods`，不要把财务期间或财务指标放进 `evidence_ids`。
- 008 的 `fact_ledger.financial_quality` 可从 `financial_evidence_pack` 读取 `latest_period`、`financial_flags` 数量和 `financial_data_gaps`，用于解释数据置信度；模型输出中的评分口径仍由服务层覆盖，避免模型自由打分。
- 010 无锚定估值实验室后续应从同一份 `financial_evidence_pack` 派生 `valuation_inputs`，例如 `base_free_cash_flow`、`base_net_profit`、增长率、利润率、ROIC、资本开支、分红、回购、现金、有息负债和股本。010 可以读取 008 的估值假设建议，但估值计算本身应以 005 的财务事实和确定性指标为底座。
- 010 保存估值 run 时应记录使用的 `financial_evidence_pack` 摘要或 hash、核心估值输入和 `price_blind=true`，方便未来复核“当时用了哪版财务底稿”。
- 011 只能读取已经锁定的 010 估值结果，再引入当前价格、市值、历史估值分位和持仓成本做安全边际与动作判断。价格相关字段不得回流到 005 的财务证据包，也不得进入 010 的无锚定估值输入。

## 010 估值实验室待用财务指标

010 无锚定估值实验室后续应从 005 的财务证据包读取基本面输入，禁止读取当前价格、市值、历史价格、持仓成本、券商评级和市场情绪。

估值基础事实：

- `base_revenue`：基准营业收入，通常取最近一年或 TTM。
- `base_gross_profit`：基准毛利。
- `base_net_profit`：基准归母净利润。
- `base_deducted_net_profit`：基准扣非净利润，用于排除一次性收益干扰。
- `base_operating_cash_flow`：基准经营现金流。
- `base_free_cash_flow`：基准自由现金流，优先使用经营现金流减资本开支。
- `capital_expenditure`：资本开支，用于判断维持增长所需再投资。
- `depreciation_and_amortization`：折旧摊销，用于自由现金流和所有者盈余口径。
- `working_capital_change`：营运资本变化，用于现金流可持续性判断。

增长和质量假设：

- `revenue_growth_rate`：收入增长率，可拆为保守/中性/乐观。
- `net_profit_growth_rate`：净利润增长率。
- `free_cash_flow_growth_rate`：自由现金流增长率。
- `revenue_cagr_3y/5y`：收入 3/5 年复合增长率。
- `net_profit_cagr_3y/5y`：净利润 3/5 年复合增长率。
- `free_cash_flow_cagr_3y/5y`：自由现金流 3/5 年复合增长率。
- `gross_margin`、`net_margin`：毛利率和净利率，用于利润率情景假设。
- `roe`、`roic`：资本回报质量，用于合理倍数和再投资效率判断。

资产负债调整：

- `cash_and_equivalents`：货币资金或可加回现金。
- `interest_bearing_debt`：有息负债，估值时从经营价值中扣除。
- `net_debt`：净负债。
- `shareholders_equity`：股东权益，支持剩余收益模型。
- `total_assets`、`total_liabilities`：总资产和总负债，支持资产价值与安全边际检查。
- `goodwill`：商誉，用于资产质量折扣和减值风险判断。
- `receivables`、`inventory`：应收账款和存货，用于营运质量、资产质量和清算价值折扣。

股东回报和每股口径：

- `dividend`：分红总额。
- `dividend_payout_ratio`：分红率。
- `buyback_amount`：回购金额。
- `shares_outstanding`：总股本，用于把股权价值换算成每股内在价值。
- `share_dilution_rate`：股本稀释率。

估值模型用途：

- DCF：重点使用 `base_free_cash_flow`、未来增长率、永续增长率、折现率、资本开支和营运资本变化。
- 所有者盈余：重点使用净利润、折旧摊销、资本开支、营运资本变化和一次性项目调整。
- 分红折现：重点使用分红总额、分红率、分红增长率和股本。
- 剩余收益：重点使用股东权益、ROE、权益资本成本和利润留存。
- 资产价值/清算价值：重点使用现金、有息负债、应收、存货、固定资产、商誉、总负债和股东权益。
- 合理倍数估值：重点使用增长质量、ROIC、利润率稳定性、现金流质量和财务安全指标。

## 验证入口

常用验证命令：

- 后端测试：`cd backend; .\.venv\Scripts\python.exe -m pytest`
- 后端 lint：`cd backend; .\.venv\Scripts\python.exe -m ruff check .`
- 前端测试：`cd frontend; npm test -- --run`
- 前端构建：`cd frontend; npm run build`

最近一次验证：

- 2026-08-14：`cd backend; .\.venv\Scripts\python.exe -m pytest` 通过，`89 passed`。
- 2026-08-14：`cd backend; .\.venv\Scripts\python.exe -m ruff check .` 通过。
- 2026-08-14：`cd frontend; npm test -- --run` 通过，`32 passed`。
- 2026-08-14：`cd frontend; npm run build` 通过。

运行态检查：

- `http://127.0.0.1:8000/openapi.json` 应包含：
  - `/api/companies/{company_id}/financials`
  - `/api/companies/{company_id}/financials/sync`
  - `/api/companies/{company_id}/financials/{statement_id}`
- OpenAPI 中财务读取和同步默认条数应为 `60`。

## 2026-08-14 财务证据包 v1 实装记录

本次把 005 的财务内容从“原始期间列表”升级为可复用的结构化 `financial_evidence_pack`。它仍然只基于本地已入库的 `financial_statements` 生成，不读取公告、不读取外部 Evidence、不读取价格和市值。

新增后端能力：

- 新增 `backend/app/services/financial_metrics.py`，集中负责财务记录排序、财务事实抽取、确定性指标计算、趋势计算、财务旗标和数据缺口生成。
- 新增 `GET /api/companies/{company_id}/financials/evidence-pack`，前端和后续模块可以直接读取本地财务证据包。
- `GET /api/companies/{company_id}/financials` 和 008 快照统一复用 `fields.report_date`、`period`、`id` 倒序排序，避免中文期间字符串把中报、三季报、年报排乱。
- 当前证据包包含 `latest_period`、`periods`、`financial_facts`、`financial_metrics`、`financial_trends`、`financial_flags`、`financial_data_gaps`。

当前证据包字段分层：

- `financial_facts.latest`：最近一期绝对金额事实，当前覆盖 `revenue`、`gross_profit`、`net_profit`、`deducted_net_profit`。
- `financial_facts.series`：多期事实序列，用于后续趋势、可视化和估值基准选择。
- `financial_metrics.profitability`：`roe`、`gross_margin`、`net_margin`。
- `financial_metrics.cash_quality`：`operating_cash_flow_to_revenue`。
- `financial_metrics.growth_quality`：`revenue_yoy`、`net_profit_yoy`。
- `financial_metrics.balance_sheet_safety`：`asset_liability_ratio`。
- `financial_metrics.efficiency`：`total_assets_turnover`、`inventory_turnover_days`。
- `financial_metrics.per_share`：`eps`、`bps`、`operating_cash_flow_per_share`。
- `financial_trends`：当前计算收入和净利润 3/5 年 CAGR，以及 ROE、毛利率、净利率稳定性。
- `financial_flags`：当前覆盖收入下滑、利润下滑、利润增速显著落后收入增速、高负债率、现金流/收入偏低、利润率下滑等确定性旗标。
- `financial_data_gaps`：标出 010 估值实验室仍需要但当前 F10 主财务指标不一定提供的字段，例如经营现金流、资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本等。

给 008 分析师视角的使用方式：

- 008 的 `build_company_analysis_snapshot(...)` 现在同时传入原始 `financial_statements` 和整理后的 `financial_evidence_pack`。
- 分析师模型优先使用 `financial_evidence_pack` 形成 `financial_observations`、`risk_flags`、`data_gaps`、`valuation_assumption_suggestions`。
- 原始 `financial_statements` 仍用于追溯具体期间；模型引用财务证据时继续写入 `rule_checks[].financial_periods`，不要把财务期间或财务指标放进 `evidence_ids`。
- 008 的 `fact_ledger.financial_quality` 从证据包读取 `latest_period`、`financial_flags` 数量和 `financial_data_gaps`，用于解释数据置信度。
- `backend/app/analysis/prompts/analyst.py` 已明确提示：`financial_evidence_pack` 是后端确定性财务证据包，`financial_statements` 是原始期间记录。

给 010 估值实验室的后续使用方式：

- 010 应从同一个 `financial_evidence_pack` 派生 `valuation_inputs`，而不是让模型现场从原始字段猜公式。
- 估值输入优先使用 `financial_facts.latest` 和稳定的多年序列选择基准收入、基准净利润、基准自由现金流。
- 增长假设优先来自 `financial_trends`，再结合 008 的 `valuation_assumption_suggestions` 做保守/中性/乐观三档。
- 折现率、永续增长率、再投资需求和资产负债调整要结合 `financial_metrics`、`financial_flags`、`financial_data_gaps` 决定置信度。
- 010 阶段必须继续保持 `price_blind=true`，禁止读取当前价格、市值、历史价格、持仓成本、市场情绪或评级。
- 010 保存估值 run 时应记录所用 `financial_evidence_pack` 摘要或 hash、核心 `valuation_inputs` 和 `price_blind=true`，方便未来复核当时使用的财务底稿。

估值实验室后续仍需补齐的关键字段：

- 现金流：`operating_cash_flow`、`capital_expenditure`、`free_cash_flow`、`depreciation_and_amortization`、`working_capital_change`。
- 资产负债：`cash_and_equivalents`、`interest_bearing_debt`、`total_assets`、`total_liabilities`、`shareholders_equity`、`goodwill`、`receivables`、`inventory`。
- 资本回报：`roic`、投入资本、税后经营利润相关口径。
- 股东回报：`dividend`、`dividend_payout_ratio`、`buyback_amount`、`shares_outstanding`、`share_dilution_rate`。

本次验证：

- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" app\tests\test_analysis.py -k "financial or snapshot or analyst_view_records"` 通过，`18 passed`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check` 未全量通过，失败点在既有 `app/services/evidence_service.py` 两条 E501 长行，非本次 005/008 改动路径。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\financial_metrics.py app\services\companies.py app\services\analyst_service.py app\schemas\company.py app\api\routes\companies.py app\analysis\prompts\analyst.py app\tests\test_companies.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest` 未全量通过，失败点在既有 007 Evidence 搜索兜底统计用例 `test_evidence_search_creates_official_search_leads_when_fallback_has_no_candidates`，`98 passed, 1 failed`。
- `cd frontend; npm test -- --run` 通过，`32 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 前端 Financials 分区

本次只调整前端展示组织，不改动 005 后端财务读取、同步、删除和 `financial_evidence_pack` 接口。

变化：

- 财务面板从公司工作台长页面中收敛到 `Financials` 分区。
- 侧边栏 `Financials` 可直接打开当前公司的财务分区；未选中公司时先进入空工作台提示选择公司。
- `Overview` 研究准备度会读取已加载财务记录和 `financial_evidence_pack`，展示财务记录数量、最新财务期和财务缺口数量。
- 原有“搜索财务数据”、财务指标展示、证据包摘要、财务删除流程继续在 `Financials` 分区内工作。

本次前端验证：

- `cd frontend; npm test -- --run` 通过，财务搜索、刷新、删除和删除失败用例均已更新到 `Financials` 分区。
- `cd frontend; npm run build` 通过。

## 2026-08-15 现金流缺口口径修正

用户在贵州茅台巴菲特视角输出中发现“缺口”写了缺少经营现金流绝对值，但财务数据里其实已有现金流相关指标。复核后确认：当前东方财富 F10 主财务指标已入库 `operating_cash_flow_per_share`（每股经营现金流）和 `operating_cash_flow_to_revenue`（经营现金流/收入），但不一定提供 `operating_cash_flow` 绝对金额。

本次修正 005 的 `financial_evidence_pack`：
- `financial_facts.latest/series` 增加 `operating_cash_flow`，如果后续现金流量表或数据源补齐绝对值，证据包会直接传递。
- 新增 `cash_flow_coverage`，记录 `has_operating_cash_flow`、`has_cash_flow_proxy`、`proxy_fields` 和说明性 `note`。
- `financial_data_gaps` 对经营现金流做分层表达：
  - 如果既没有绝对经营现金流，也没有每股现金流或现金流/收入代理指标，才写“缺少经营现金流绝对值，无法计算经营现金流/净利润。”
  - 如果已有代理指标但缺绝对值，写“缺少经营现金流绝对值；当前已有每股经营现金流或经营现金流/收入等代理口径，严格估值前需补充现金流量表绝对值或总股本复核。”
- 这样 008 分析师不会再把“已有现金流代理口径但缺严格绝对值”误读成完全没有现金流数据；010 估值实验室后续仍应优先补齐现金流量表绝对值、资本开支和自由现金流。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`9 passed, 39 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\services\financial_metrics.py app\analysis\prompts\analyst.py app\tests\test_analysis.py app\tests\test_companies.py` 通过。

## 2026-08-15 估值关键财务字段补齐方案

用户确认：资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本等字段需要采集并加入财务记录，用于填补 `financial_data_gaps`，同时给 008 分析师和后续 010 估值实验室提供更完整输入。

建议 005 下一步补齐的数据层：

- 现金流量表字段：`operating_cash_flow`、`capital_expenditure`、`free_cash_flow`、`depreciation_and_amortization`、`working_capital_change`。
- 资产负债表字段：`cash_and_equivalents`、`short_term_interest_bearing_debt`、`long_term_interest_bearing_debt`、`interest_bearing_debt`、`total_assets`、`total_liabilities`、`shareholders_equity`、`goodwill`、`receivables`、`inventory`。
- 股东回报字段：`dividend`、`dividend_payout_ratio`、`buyback_amount`、`shares_outstanding`、`share_dilution_rate`。
- 派生估值底稿字段：`free_cash_flow`、`net_cash`、`cash_to_interest_bearing_debt`、`operating_cash_flow_to_net_profit`、`free_cash_flow_to_net_profit`、`free_cash_flow_margin`、`dividend_yield_on_profit`。

建议入库方式：

- 继续复用 `financial_statements` 表的 JSON `fields`，不急于新增专门表，避免迁移复杂度过高。
- 使用不同 `statement_type` 区分来源口径，例如 `main_financial_indicators`、`cash_flow_statement`、`balance_sheet`、`shareholder_return`、`capital_structure`。
- upsert key 仍保持 `company_id + period + statement_type`，并保留 `source`、`source_url`、`fields.report_date`，方便追溯。
- 对资本开支建议同时保存原始现金流项目和标准化字段：原始项可命名为 `purchase_fixed_assets_cash_paid`，标准化 `capital_expenditure` 统一为正数；`free_cash_flow = operating_cash_flow - capital_expenditure`。
- `interest_bearing_debt` 建议由短期借款、一年内到期非流动负债、长期借款、应付债券、租赁负债等有息项目加总，并保留分项，避免只给一个黑箱合计。
- 分红和回购优先来自公告/年报结构化字段或专门接口；如果来自公告，005 只保存财务字段结果，不接管 006 公告正文处理边界。

证据包 v2 建议结构：

- `financial_facts.latest/series` 扩展纳入收入、毛利、净利润、扣非净利润、经营现金流、资本开支、自由现金流、现金、有息负债、净现金、总资产、股东权益、分红、回购、总股本。
- `financial_metrics.cash_quality` 扩展为 `operating_cash_flow_to_net_profit`、`free_cash_flow_to_net_profit`、`free_cash_flow_margin`、`operating_cash_flow_to_revenue`。
- `financial_metrics.balance_sheet_safety` 扩展为 `asset_liability_ratio`、`cash_to_interest_bearing_debt`、`net_cash`、`interest_bearing_debt_to_equity`。
- `financial_metrics.shareholder_return` 新增 `dividend_payout_ratio`、`buyback_ratio`、`share_dilution_rate`。
- `financial_trends` 扩展 `free_cash_flow_cagr_3y/5y`、`capital_expenditure_to_revenue_trend`、`interest_bearing_debt_trend`、`dividend_stability`。
- `financial_data_gaps` 从“缺字段列表”升级为带结构的缺口对象，包含 `field`、`severity`、`reason`、`needed_by`、`replacement_available`，其中 `needed_by` 可标记 `analyst_view` 或 `valuation_lab`。

完成这些字段后，008 分析师不应再只看到“缺少资本开支/自由现金流/有息负债”等粗缺口，而应获得可直接判断财务质量和估值假设的完整财务底稿。010 估值实验室再从同一份证据包派生 `valuation_inputs`，保持 `price_blind=true`。

## 2026-08-15 财务字段补齐 goal 开发 prompt

已为后续 goal 模式准备开发 prompt，目标是按“估值关键财务字段补齐方案”实现 005 财务采集扩展、`financial_evidence_pack` v2、008 分析师输入优化和相关测试。该 prompt 要求开始前阅读 `memory/001_价值投资分析师系统架构.md`、`memory/005_公司详情财务.md`、`memory/008_分析师视角引擎.md`，保持 005 不接管公告正文处理、008 不主动搜索、010 继续 `price_blind=true`。

## 2026-08-15 财务字段补齐与 evidence pack v2 实装记录

本次按“资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本需要采集并加入财务记录”的方案继续推进 005。边界仍保持：005 只负责财务数据采集、字段标准化、入库和确定性指标计算；不接管 006 公告正文解析，不读取价格、市值或投资建议。

数据源扩展：
- `backend/app/data_sources/eastmoney_financials.py` 新增 `fetch_financials(...)`，一次同步东方财富 F10 主要财务指标、现金流量表和资产负债表。
- 现金流量表使用 `RPT_F10_FINANCE_GCASHFLOW`，本地 `statement_type="cash_flow_statement"`。
- 资产负债表使用 `RPT_F10_FINANCE_GBALANCE`，本地 `statement_type="balance_sheet"`。
- `sync_company_financials(...)` 优先调用新版 `fetch_financials(...)`，同时保留旧 fake client 的 `fetch_main_financials(...)` 兼容路径。

新增或标准化字段：
- 现金流：`operating_cash_flow`、`purchase_fixed_assets_cash_paid`、`capital_expenditure`、`free_cash_flow`、`depreciation_and_amortization`、`working_capital_change`、`dividend`。
- 资产负债：`cash_and_equivalents`、`short_term_interest_bearing_debt`、`long_term_interest_bearing_debt`、`interest_bearing_debt`、`net_cash`、`total_assets`、`total_liabilities`、`shareholders_equity`、`goodwill`、`receivables`、`inventory`、`shares_outstanding`。
- 回购：当前东方财富资产负债表可取得 `treasury_shares`，作为 `buyback_amount_proxy` 代理口径；严格 `buyback_amount` 仍作为结构化缺口保留，后续可从公告结构化字段或专门接口补齐。

`financial_evidence_pack` v2：
- 按 `period` 合并同一期多条 `financial_statements`，保留 `statement_types` 和 `source_statement_ids`，让主财务指标、现金流量表、资产负债表共同形成同一期财务底稿。
- 后端确定性派生 `free_cash_flow`、`net_cash`、`asset_liability_ratio`、`operating_cash_flow_to_net_profit`、`free_cash_flow_to_net_profit`、`free_cash_flow_margin`、`cash_to_interest_bearing_debt`、`interest_bearing_debt_to_equity`、`dividend_payout_ratio`、`buyback_ratio`、`capital_expenditure_to_revenue`。
- `financial_facts.latest/series` 已扩展收入、毛利、净利润、扣非净利润、经营现金流、资本开支、自由现金流、货币资金、有息负债、净现金、总资产、总负债、股东权益、分红、回购、总股本。
- `financial_metrics` 已扩展 `cash_quality`、`balance_sheet_safety`、`shareholder_return`、`capital_allocation`。
- `financial_trends` 已扩展自由现金流 CAGR、资本开支/收入趋势、有息负债趋势、分红稳定性、股本趋势。
- `financial_data_gaps` 从字符串升级为结构化对象，包含 `field`、`severity`、`reason`、`needed_by`、`replacement_available` 和可选 `proxy_fields`；同时保留 `financial_data_gap_messages` 便于旧展示降级。
- 新增分析师和估值实验室友好字段：`analyst_summary`、`quality_matrix`、`cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness`、`data_quality`。

前端兼容：
- `FinancialEvidencePack` 类型已兼容结构化缺口和 v2 新字段。
- `Financials` 分区的财务证据概览新增自由现金流、货币资金、有息负债、净现金、分红和估值准备度展示；旧财务搜索、刷新、删除流程保持不变。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check .` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`10 passed, 41 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`26 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m pytest` 通过，`117 passed, 1 warning`。
- `cd frontend; npm test -- --run` 通过，`40 passed`。
- `cd frontend; npm run build` 通过。

后续限制：
- `buyback_amount` 仍未获得严格现金金额口径，当前只用 `treasury_shares/buyback_amount_proxy` 标记代理可用。
- `roic`、投入资本和税后经营利润相关口径尚未在本轮补齐，后续可在更完整三张表或公告结构化数据稳定后加入。
## 2026-08-15 财务前端展示与十年输入口径修正

本次继续优化 005 财务展示和后续分析输入口径，目标是让前端更易读，但不改变数据库中保存的精细原始值。

前端 Financials 展示调整：
- 财务报表字段和报表类型继续使用中文展示，新增资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本、股东回报、资本结构等字段中文名。
- 金额类字段只在前端展示时转成大单位，例如 `922.78亿 CNY`；保存到 `financial_statements.fields` 的仍然是原始精细数值，例如 `92278000000`。
- 来源字段增加中文映射，例如 `eastmoney_f10_main_finance` 展示为“东方财富 F10 主要财务指标”，不影响入库 source 原值。
- 财务报表列表改为两栏网格展示，移动端仍回到单栏，避免三张表扩展后 100 多条记录纵向过长。

后续分析输入口径：
- 008 分析师快照不再按“最近 40 条 financial_statements 记录”截断。
- 新口径为“最近 40 个财务期间”，并保留每个期间下的全部报表类型，例如主财务指标、现金流量表、资产负债表同时存在时，40 期可以对应约 120 条记录。
- 这保证三张表拆分后仍然覆盖十年左右的财务历史，而不会因为每期有三条记录而只剩三年多。

本次验证：
- `cd frontend; npm test -- --run` 通过，`40 passed`。
- `cd frontend; npm run build` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`27 passed, 1 warning`。
## 2026-08-15 同一报告期报表展示顺序

本次只调整前端 Financials 报表列表的展示顺序，不改变后端入库、同步、删除或 `financial_evidence_pack` 生成逻辑。

展示规则：
- 不同报告期之间继续沿用接口返回顺序，通常是最新报告期在前。
- 同一报告期内按固定报表类型顺序展示：
  1. `main_financial_indicators`：主要财务指标
  2. `cash_flow_statement`：现金流量表
  3. `balance_sheet`：资产负债表
- 其他未知或后续新增的 `statement_type` 保持原始相对顺序，排在上述三类之后，避免破坏扩展兼容性。

实现位置：
- `frontend/src/app/CompanyWorkspaceView.tsx` 新增前端展示排序函数 `sortFinancialStatementsForDisplay(...)`。
- `frontend/src/tests/App.test.tsx` 在财务搜索用例中加入同一报告期乱序返回的三张表，验证页面展示顺序会被修正为“主要财务指标、现金流量表、资产负债表”。

本次验证：
- `cd frontend; npm test -- --run` 通过，`40 passed`。
- `cd frontend; npm run build` 通过。
## 2026-08-15 经营现金流/收入比例单位修正

用户指出“经营现金流/收入比例”的单位/口径可能错误。复核后确认：
- 前端展示层已把 `operating_cash_flow_to_revenue` 识别为比例字段，正常内部值 `0.4862` 会展示为 `48.6%`，不应展示为金额或 `CNY`。
- 后端原先对东方财富 `JYXJLYYSR` 一律按百分数字段除以 100；如果数据源返回 `48.62`，会正确入库为 `0.4862`，但如果数据源直接返回 `0.4862`，会被误归一化为 `0.004862`，前端显示成 `0.5%`。

本次修正：
- `backend/app/data_sources/eastmoney_financials.py` 中 `JYXJLYYSR -> operating_cash_flow_to_revenue` 改为兼容两种口径：
  - 绝对值大于 `1`：按百分数处理，除以 `100`。
  - 绝对值小于等于 `1`：视为已经是内部比例值，直接保留。
- 其他百分比字段仍使用原来的 `_percent_to_ratio(...)`，避免把净利率、ROE 等低百分数字段误判为比例。
- 新增测试覆盖 `JYXJLYYSR="0.4862"` 时应入库为 `0.4862`，避免二次除以 100。

影响：
- 重新同步财务数据后，`operating_cash_flow_to_revenue` 会统一保存为内部比例值，前端展示为百分比，例如 `48.6%`。
- 已经历史入库且被错误二次除以 100 的记录，需要通过重新点击“搜索财务数据”触发 upsert 更新，或后续单独做一次数据修复。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`11 passed, 41 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\data_sources\eastmoney_financials.py app\tests\test_companies.py` 通过。

## 2026-08-15 财务记录 ID 前端展示

本次按用户要求在 Financials 每条财务记录卡片右下角展示数据库记录 ID，格式为 `#<id>`，例如 `#2`。该 ID 直接来自 `financial_statements.id`，用于用户在前端快速定位、反馈或删除具体财务记录。

实现说明：
- `frontend/src/app/CompanyWorkspaceView.tsx` 在每个 `financial-statement` 卡片底部新增 `financial-statement-footer`，左侧继续展示来源，右侧展示 `record-id`。
- `frontend/src/styles/global.css` 将财务卡片改为纵向 flex 布局，并让 footer 使用 `space-between`，保证 `#id` 稳定位于卡片右下角。
- `frontend/src/tests/App.test.tsx` 在财务数据搜索/刷新用例中新增 `#2`、`#4`、`#5` 展示断言。

数据影响：
- 不改变后端接口、入库结构、同步逻辑或 `financial_evidence_pack`。
- 前端显示的是追溯用记录 ID；传给后续分析师和估值实验室的财务证据包逻辑不受影响。

本次验证：
- `cd frontend; npm test -- --run` 通过，`41 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 利润表补齐判断

用户指出当前财务数据只有主要财务指标、现金流量表和资产负债表，询问利润表是否作用不大。结论：利润表作用很大，当前不是因为利润表不重要而不采集，而是此前先用东方财富 F10 主要财务指标覆盖了收入、毛利、净利润、扣非净利润、毛利率、净利率、ROE 等结果项，用来满足 MVP 和估值基准的第一层需求。

利润表仍应作为 005 的下一步核心补齐项：
- 主要财务指标能回答“结果怎么样”，但不能充分回答“利润从哪里来、成本费用怎么变、利润是否可持续”。
- 利润表应新增 `statement_type="income_statement"`，与 `main_financial_indicators`、`cash_flow_statement`、`balance_sheet` 同期合并进 `financial_evidence_pack`。
- 同一报告期的前端推荐展示顺序后续可调整为：主要财务指标、利润表、现金流量表、资产负债表。
- 利润表重点字段建议包括：营业收入、营业成本、毛利、税金及附加、销售费用、管理费用、研发费用、财务费用、其他收益、投资收益、公允价值变动收益、信用减值损失、资产减值损失、营业利润、营业外收入、营业外支出、利润总额、所得税费用、净利润、归母净利润、少数股东损益、扣非净利润、基本 EPS。
- 派生指标建议包括：毛利率、营业利润率、净利率、销售费用率、管理费用率、研发费用率、财务费用率、期间费用率、有效税率、营业利润/净利润、减值损失/净利润、投资收益/净利润。

给 008 分析师的价值：
- 判断商业模式和护城河：毛利率、费用率、研发投入、财务费用趋势能解释定价权、规模效应和竞争压力。
- 判断利润质量：营业利润、投资收益、减值损失、营业外收支与净利润的关系能识别一次性收益或利润粉饰。
- 判断风险：成本上升、费用失控、减值扩大、财务费用上升都应进入 `financial_flags` 和分析师风险点。

给 010 估值实验室的价值：
- DCF 和所有者盈余需要利润表拆分帮助判断正常化利润率、税率、折旧摊销和再投资需求。
- 剩余收益模型需要净利润、股东权益和 ROE 的可持续性判断。
- 合理倍数估值需要用利润率稳定性、费用率趋势、非经常性利润占比来校准估值倍数。

边界：
- 005 只采集和标准化利润表结构化字段，不接管 006 公告正文解析，不读取价格、市值或投资建议。
- 如果东方财富利润表接口可直接取得，优先作为结构化数据源；如果部分利润表明细只能从公告解析获得，则由后续 006 输出结构化字段后再回填或合并到 005 证据包。

## 2026-08-15 加入利润表后的分析师输入字段调整判断

如果 005 后续新增 `statement_type="income_statement"`，传给后续 008 分析师的原始财务记录通道不需要重做，但 `financial_evidence_pack` 的结构化字段需要扩展。否则利润表只会作为一条原始 JSON 记录堆给模型，分析师仍然不能稳定利用费用率、减值、投资收益和非经常性损益等关键判断素材。

不需要改的部分：
- `financial_statements` 继续作为原始期间记录输入，用于追溯具体记录和写入 `rule_checks[].financial_periods`。
- 008 的截断口径继续保持“最近 40 个财务期间”，每个期间保留全部 `statement_type`。利润表加入后，40 期大约会从三张表的约 120 条记录变成四张表的约 160 条记录，不应退回按 40 条记录截断。
- 入库结构仍可复用 `financial_statements.fields` JSON，不必为了利润表单独新建表。

建议修改的 005 证据包字段：
- `financial_facts.latest/series` 增加利润表分项：`operating_cost`、`taxes_and_surcharges`、`selling_expense`、`admin_expense`、`r_and_d_expense`、`finance_expense`、`other_income`、`investment_income`、`fair_value_change_income`、`credit_impairment_loss`、`asset_impairment_loss`、`operating_profit`、`non_operating_income`、`non_operating_expense`、`total_profit`、`income_tax_expense`、`minority_interest`。
- `financial_metrics` 新增或扩展 `profit_structure`：`operating_margin`、`operating_profit_to_net_profit`、`deducted_net_profit_to_net_profit`、`investment_income_to_net_profit`、`fair_value_change_to_net_profit`、`impairment_loss_to_net_profit`、`non_operating_profit_to_net_profit`、`effective_tax_rate`。
- `financial_metrics` 新增或扩展 `expense_control`：`selling_expense_ratio`、`admin_expense_ratio`、`r_and_d_expense_ratio`、`finance_expense_ratio`、`period_expense_ratio`。
- `financial_trends` 增加利润表趋势：`operating_margin_stability`、`period_expense_ratio_trend`、`r_and_d_expense_ratio_trend`、`finance_expense_ratio_trend`、`investment_income_to_net_profit_trend`、`impairment_loss_to_net_profit_trend`、`effective_tax_rate_trend`。
- `financial_flags` 增加利润表风险旗标：毛利率或营业利润率下滑、期间费用率上升、财务费用率上升、投资收益/公允价值变动占利润过高、减值损失占利润过高、营业外收支占利润过高、所得税率异常、扣非净利润显著弱于归母净利润。
- `financial_data_gaps` 增加利润表缺口对象，例如 `income_statement`、`operating_cost`、`expense_breakdown`、`operating_profit`、`impairment_losses`、`non_operating_items`、`income_tax_expense`；如果主要财务指标已提供收入/净利润等结果项，可设置 `replacement_available=true`，但仍说明缺少利润构成明细会降低利润质量判断置信度。
- `quality_matrix` 增加 `profit_structure`、`expense_control`、`accounting_quality` 三块。
- `analyst_summary` 增加 `income_statement_quality` 或 `profit_composition`，把主营利润、费用纪律、非经常性项目、减值和税率异常压缩成分析师优先阅读摘要。

给 008 分析师快照的调整：
- `source_boundary.selection_policy.financial_statements` 文案应从“主要财务指标、现金流量表和资产负债表”扩展为包含利润表。
- `source_coverage_matrix.financial` 应能标记 `profit_structure`、`expense_control`、`accounting_quality` 等主题覆盖。
- `fact_ledger.financial_quality` 建议增加 `income_statement_coverage`，例如是否有利润表、是否有费用明细、是否有减值和非经常性项目。
- `pre_model_observations` 应从利润表旗标生成确定性提示，例如“投资收益占净利润较高”“费用率连续上升”“扣非净利润弱于归母净利润”。
- `backend/app/analysis/prompts/analyst.py` 应提示模型优先读取新增的 `profit_structure`、`expense_control`、`income_statement_quality`，不要从原始利润表 JSON 现场猜公式。

给 010 估值实验室的衔接：
- 利润表加入后，010 的 `valuation_inputs` 可以更稳地派生 `base_normalized_net_profit`、`normalized_operating_margin`、`effective_tax_rate`、`non_recurring_profit_adjustment`、`impairment_adjustment`、`owner_earnings_adjustment`。
- 对 DCF、所有者盈余、剩余收益、合理倍数估值都有帮助，尤其是把“表观净利润”调整为“可持续经营利润”。

## 2026-08-15 利润表补齐后的分析师财务输入字段清单

如果后续按利润表方案完成修改，008 分析师收到的财务数据仍保持两层：`financial_statements` 原始追溯记录 + `financial_evidence_pack` 结构化证据包。

`financial_statements[]` 每条记录字段：
- `id`、`period`、`statement_type`、`currency`、`fields`、`source`、`source_url`、`created_at`。
- `statement_type` 包含：`main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`，后续可扩展 `shareholder_return`、`capital_structure`。

`financial_statements[].fields` 通用字段：
- `report_date`、`report_type`、`notice_date`、`raw_secucode`、`raw_security_name`。

`main_financial_indicators.fields`：
- `revenue`、`gross_profit`、`net_profit`、`deducted_net_profit`、`eps`、`bps`、`roe`、`gross_margin`、`net_margin`、`asset_liability_ratio`、`revenue_yoy`、`net_profit_yoy`、`operating_cash_flow_per_share`、`operating_cash_flow_to_revenue`、`total_assets_turnover`、`inventory_turnover_days`。

`income_statement.fields`：
- `revenue`、`operating_cost`、`gross_profit`、`taxes_and_surcharges`、`selling_expense`、`admin_expense`、`r_and_d_expense`、`finance_expense`、`other_income`、`investment_income`、`fair_value_change_income`、`credit_impairment_loss`、`asset_impairment_loss`、`operating_profit`、`non_operating_income`、`non_operating_expense`、`total_profit`、`income_tax_expense`、`net_profit`、`parent_net_profit`、`minority_interest`、`deducted_net_profit`、`eps`。

`cash_flow_statement.fields`：
- `operating_cash_flow`、`purchase_fixed_assets_cash_paid`、`capital_expenditure`、`free_cash_flow`、`depreciation_and_amortization`、`working_capital_change`、`dividend`、`net_cash_from_investing`、`net_cash_from_financing`。

`balance_sheet.fields`：
- `cash_and_equivalents`、`short_term_interest_bearing_debt`、`long_term_interest_bearing_debt`、`interest_bearing_debt`、`net_cash`、`total_assets`、`total_liabilities`、`shareholders_equity`、`total_equity`、`goodwill`、`receivables`、`inventory`、`shares_outstanding`、`treasury_shares`、`buyback_amount_proxy`、`dividend_payable`。

`financial_evidence_pack` 顶层字段：
- `latest_period`、`periods`、`financial_facts`、`financial_metrics`、`cash_flow_coverage`、`financial_trends`、`financial_flags`、`financial_data_gaps`、`financial_data_gap_messages`、`cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness`、`quality_matrix`、`analyst_summary`、`data_quality`。

`financial_facts.latest` 与 `financial_facts.series` 字段：
- `revenue`、`gross_profit`、`net_profit`、`deducted_net_profit`、`operating_cash_flow`、`capital_expenditure`、`free_cash_flow`、`cash_and_equivalents`、`interest_bearing_debt`、`net_cash`、`total_assets`、`total_liabilities`、`shareholders_equity`、`dividend`、`buyback_amount`、`shares_outstanding`。
- 利润表补齐后增加：`operating_cost`、`taxes_and_surcharges`、`selling_expense`、`admin_expense`、`r_and_d_expense`、`finance_expense`、`other_income`、`investment_income`、`fair_value_change_income`、`credit_impairment_loss`、`asset_impairment_loss`、`operating_profit`、`non_operating_income`、`non_operating_expense`、`total_profit`、`income_tax_expense`、`minority_interest`。

`financial_metrics` 字段：
- `profitability`：`roe`、`gross_margin`、`net_margin`。
- `cash_quality`：`operating_cash_flow_to_revenue`、`operating_cash_flow_to_net_profit`、`free_cash_flow_to_net_profit`、`free_cash_flow_margin`。
- `growth_quality`：`revenue_yoy`、`net_profit_yoy`。
- `balance_sheet_safety`：`asset_liability_ratio`、`cash_to_interest_bearing_debt`、`net_cash`、`interest_bearing_debt_to_equity`。
- `efficiency`：`total_assets_turnover`、`inventory_turnover_days`。
- `per_share`：`eps`、`bps`、`operating_cash_flow_per_share`。
- `shareholder_return`：`dividend_payout_ratio`、`buyback_ratio`、`share_dilution_rate`。
- `capital_allocation`：`capital_expenditure_to_revenue`、`dividend`、`buyback_amount`、`total_shareholder_return`。
- 利润表补齐后增加 `profit_structure`：`operating_margin`、`operating_profit_to_net_profit`、`deducted_net_profit_to_net_profit`、`investment_income_to_net_profit`、`fair_value_change_to_net_profit`、`impairment_loss_to_net_profit`、`non_operating_profit_to_net_profit`、`effective_tax_rate`。
- 利润表补齐后增加 `expense_control`：`selling_expense_ratio`、`admin_expense_ratio`、`r_and_d_expense_ratio`、`finance_expense_ratio`、`period_expense_ratio`。

`financial_trends` 字段：
- `revenue_cagr_3y`、`revenue_cagr_5y`、`net_profit_cagr_3y`、`net_profit_cagr_5y`、`free_cash_flow_cagr_3y`、`free_cash_flow_cagr_5y`、`roe_stability`、`gross_margin_stability`、`net_margin_stability`、`capital_expenditure_to_revenue_trend`、`interest_bearing_debt_trend`、`dividend_stability`、`shares_outstanding_trend`。
- 利润表补齐后增加：`operating_margin_stability`、`period_expense_ratio_trend`、`r_and_d_expense_ratio_trend`、`finance_expense_ratio_trend`、`investment_income_to_net_profit_trend`、`impairment_loss_to_net_profit_trend`、`effective_tax_rate_trend`。

其他结构化字段：
- `cash_flow_coverage`：`has_operating_cash_flow`、`has_cash_flow_proxy`、`proxy_fields`、`note`。
- `cash_flow_quality`：经营现金流、资本开支、自由现金流和现金流覆盖利润相关字段。
- `balance_sheet_adjustment`：现金、有息负债、净现金、总资产、总负债、股东权益、资产负债率和现金/有息负债。
- `capital_allocation`：资本开支、资本开支/收入、分红、回购、股本和股本变化。
- `valuation_readiness`：`ready_methods`、`blocked_methods`、`missing_inputs`、`method_inputs`。
- `quality_matrix`：`profitability`、`profit_structure`、`expense_control`、`accounting_quality`、`cash_flow`、`growth`、`balance_sheet`、`capital_allocation`、`shareholder_return`、`valuation_inputs`。
- `analyst_summary`：`latest_period`、`business_scale`、`profitability`、`income_statement_quality` 或 `profit_composition`、`cash_flow_quality`、`balance_sheet_safety`、`capital_allocation`、`shareholder_return`、`main_flags`、`main_data_gaps`。
- `data_quality`：覆盖期数、可用字段、缺口数量、代理口径和置信度惩罚。
- `financial_flags[]`：`code`、`severity`、`message`、`source_periods` 或同等可追溯字段。
- `financial_data_gaps[]`：`field`、`severity`、`reason`、`needed_by`、`replacement_available`、`proxy_fields`。

## 2026-08-15 利润表分步开发 prompt 方案

本次为后续 goal 模式拆成两段开发指令：
- 第一段发给 `005_公司详情财务`：添加利润表采集、入库、前端展示和 `financial_evidence_pack` 利润表字段派生。
- 第二段等 005 完成后发给 `008_分析师视角引擎`：适配分析师快照、覆盖矩阵、fact ledger、pre_model_observations 和分析师 prompt。

005 prompt 重点边界：
- 005 只做财务结构化数据采集、字段标准化、入库、展示和确定性指标计算。
- 不接管 006 公告正文解析，不读取价格、市值、历史价格、持仓成本或投资建议。
- 保持 `financial_statements` JSON 入库结构，不新增专门利润表表，upsert key 仍为 `company_id + period + statement_type`。
- 同步后同一期间应包含 `main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`。
- 修改完成后更新本文件并跑后端/前端相关测试。

## 2026-08-15 利润表采集与 evidence pack 扩展实装记录

本次按 goal objective 完成 005 利润表补齐。边界保持：005 只做财务结构化数据采集、字段标准化、入库、前端展示和确定性指标计算；不接管 006 公告正文解析；不读取价格、市值、历史价格、持仓成本、评级、市场情绪或投资建议；不新增专门利润表表，继续复用 `financial_statements.fields` JSON。

后端数据源：
- `backend/app/data_sources/eastmoney_financials.py` 新增 `fetch_income_statements(...)`。
- 东方财富 F10 利润表接口使用 `RPT_F10_FINANCE_GINCOME`，本地保存为 `statement_type="income_statement"`，来源为 `eastmoney_f10_income_statement`。
- `fetch_financials(...)` 现在同步四类记录：`main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`。
- 利润表 mapper 标准化字段包括：`revenue`、`operating_cost`、`gross_profit`、`taxes_and_surcharges`、`selling_expense`、`admin_expense`、`r_and_d_expense`、`finance_expense`、`other_income`、`investment_income`、`fair_value_change_income`、`credit_impairment_loss`、`asset_impairment_loss`、`operating_profit`、`non_operating_income`、`non_operating_expense`、`total_profit`、`income_tax_expense`、`net_profit`、`parent_net_profit`、`minority_interest`、`deducted_net_profit`、`eps`。

`financial_evidence_pack` 扩展：
- `financial_facts.latest/series` 已加入利润表分项：营业成本、税金及附加、销售/管理/研发/财务费用、其他收益、投资收益、公允价值变动、信用/资产减值、营业利润、营业外收支、利润总额、所得税费用、少数股东损益。
- `financial_metrics.profit_structure` 新增：`operating_margin`、`operating_profit_to_net_profit`、`deducted_net_profit_to_net_profit`、`investment_income_to_net_profit`、`fair_value_change_to_net_profit`、`impairment_loss_to_net_profit`、`non_operating_profit_to_net_profit`、`effective_tax_rate`。
- `financial_metrics.expense_control` 新增：`selling_expense_ratio`、`admin_expense_ratio`、`r_and_d_expense_ratio`、`finance_expense_ratio`、`period_expense_ratio`。
- `financial_trends` 新增利润表趋势：`operating_margin_stability`、`period_expense_ratio_trend`、`r_and_d_expense_ratio_trend`、`finance_expense_ratio_trend`、`investment_income_to_net_profit_trend`、`impairment_loss_to_net_profit_trend`、`effective_tax_rate_trend`。
- `financial_flags` 新增利润表质量风险：营业利润率下滑、期间费用率上升、财务费用率上升、投资收益占比高、公允价值变动占比高、减值损失占比高、营业外收支占比高、有效税率异常、扣非净利润显著弱于净利润。
- `financial_data_gaps` 新增利润表缺口：`income_statement`、`operating_cost`、`expense_breakdown`、`operating_profit`、`impairment_losses`、`non_operating_items`、`income_tax_expense`。如果已有主财务指标结果项但缺利润表，则 `income_statement` 缺口会标记 `replacement_available=true`，说明已有结果项但缺少利润构成明细。
- `quality_matrix` 新增 `profit_structure`、`expense_control`、`accounting_quality`。
- `analyst_summary` 新增 `income_statement_quality` 和 `profit_composition`，当前二者都指向利润表质量摘要，便于 008 后续平滑适配。

前端展示：
- `frontend/src/app/CompanyWorkspaceView.tsx` 已补齐利润表字段中文名和来源中文名。
- 同一报告期展示顺序改为：`main_financial_indicators` 主要财务指标、`income_statement` 利润表、`cash_flow_statement` 现金流量表、`balance_sheet` 资产负债表。
- 金额字段继续只在前端显示层转为“亿/万 + CNY”格式，保存值仍为原始精细数值。
- 每条财务记录右下角继续显示 `#id`。

测试覆盖：
- 新增东方财富利润表 mapper 测试，验证 `RPT_F10_FINANCE_GINCOME` 字段标准化。
- 同步测试已验证 `sync_company_financials(...)` 可创建四类 `statement_type`。
- `financial_evidence_pack` 测试已验证同一期四张表合并、利润表 facts、`profit_structure`、`expense_control`、`accounting_quality`、利润表 flags/gaps。
- 前端测试已验证利润表、利润表中文字段、来源中文名、金额展示和同期间四张表展示顺序。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\data_sources\eastmoney_financials.py app\services\financial_metrics.py app\services\companies.py app\tests\test_companies.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`12 passed, 42 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`27 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m pytest` 通过，`129 passed, 1 warning`。
- `cd frontend; npm test -- --run` 通过，`41 passed`。
- `cd frontend; npm run build` 通过。

仍需注意的数据源限制：
- `RPT_F10_FINANCE_GINCOME` 不直接提供严格的所有估值调整口径，例如更细的非经常性损益拆分仍可能需要 006 公告结构化数据配合。
- 减值字段在东方财富利润表中可能以 `*_LOSS` 或 `*_INCOME` 形式出现，本轮按可得字段标准化进 `credit_impairment_loss`、`asset_impairment_loss`，后续如要严格区分“损失/转回”，可增加原始字段保留或方向性说明。
- `buyback_amount`、`roic`、投入资本和税后经营利润口径仍未在本轮完全补齐，后续可结合公告结构化字段或更完整财务接口继续扩展。

## 2026-08-15 财务界面只能看到 2022 年的诊断

用户反馈财务数据界面最远只能看到 2022 年。复核本地数据库、当前前后端端口和相关代码后，结论是：不是财务数据漏采集，也不是利润表改动未入库；主要是前端财务列表和公司财务列表 API 仍按“原始报表记录条数”分页，加入利润表后同一报告期最多有四条记录，导致 `limit=60` 只能覆盖约 15 个报告期。

诊断证据：
- 本地 `data/value_investment.db` 中 `600519.SH` 已有 `240` 条财务记录、`60` 个报告期，报告日期范围为 `2011-09-30 00:00:00` 到 `2026-06-30 00:00:00`。
- `600519.SH` 同一报告期已包含 `main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet` 四类记录。
- 对本地库按当前列表排序取前 `60` 条时，只覆盖 `15` 个报告期，最旧记录为 `2022-12-31 00:00:00`，正好对应界面只能看到 2022 年。
- `GET /api/companies/4/financials?limit=60&offset=0` 返回 `total=240`、`count=60`、约 `15` 个报告期，最旧为 `2022年报`。
- `GET /api/companies/4/financials?limit=100&offset=0` 返回约 `25` 个报告期，最旧到 `2020中报`；`limit=120/200/240` 会触发 `422`，因为路由约束为 `Query(ge=1, le=100)`。
- 当前同时存在 `8001/5174` 和 `8002/5175` 两组可用服务，但两个前端代理健康检查均正常；本问题的直接根因不是端口未同步，而是列表分页语义没有随四表结构调整。

影响范围：
- 前端 `Financials` 面板受影响，因为 `frontend/src/app/CompanyWorkspaceView.tsx` 中 `FINANCIAL_STATEMENT_LIST_LIMIT = 60`，初始加载和同步后刷新都只请求 60 条原始记录，且当前没有加载更多或按报告期分页。
- 前端 `financials/evidence-pack` API 展示也受影响，因为 `backend/app/api/routes/companies.py` 当前使用 `list_company_financials(..., limit=60)` 构建展示用 evidence pack。
- 008 分析师快照自身不受前端列表限制影响：`backend/app/services/analyst_service.py` 直接从数据库读取并按 `MAX_FINANCIAL_SNAPSHOT_PERIODS = 40` 个报告期筛选，所以四表后可得到约 `160` 条原始财务记录。
- 010 估值快照需要后续注意：`backend/app/services/valuation_service.py` 当前 `limit=120` 在四表后约为 `30` 个报告期，如需覆盖 40 个报告期，应改为按报告期筛选或至少取 `160` 条原始记录。

建议修复方向：
- 不要只把前端常量从 60 改大，因为 `/financials` API 当前最大 `limit=100`，且按原始记录分页不符合“看 40 个报告期”的语义。
- 更稳妥方案是在后端提供按报告期限制的读取能力，例如 `period_limit=40` 或服务层 helper：先按 `report_date/period/id` 选出最近 N 个不同报告期，再返回这些报告期下的全部 statement types。
- 前端财务列表默认请求最近 40 个报告期对应的全部记录；如继续保留分页，应按报告期分页或增加“加载更早报告期”，避免四表扩展后再次被原始记录条数截断。
- `financials/evidence-pack`、010 估值快照也应复用同一套按报告期选取逻辑，保持前端展示、分析师输入和估值输入的财务窗口语义一致。

## 2026-08-15 财务详情按 60 报告期展示修复

本次按用户要求修复 005 财务详情显示窗口：前端 Financials 财务详情默认展示最近 15 年约 60 个报告期，并保留每个报告期下全部分表记录。以当前四张表结构计算，60 个报告期最多对应 `240` 条原始 `financial_statements` 记录。

实现要点：
- `backend/app/services/companies.py` 新增 `list_company_financials_by_periods(...)`，先按财务记录排序选出最近 N 个不同 `period`，再返回这些报告期下的全部 `statement_type` 记录。
- `GET /api/companies/{company_id}/financials` 新增可选参数 `period_limit` 和 `period_offset`；旧的 `limit/offset` 原始记录分页继续保留，避免破坏已有调用。
- 前端 `frontend/src/app/CompanyWorkspaceView.tsx` 将财务详情读取从 `limit=60` 改为 `period_limit=60&period_offset=0`。
- 前端常量拆分为 `FINANCIAL_STATEMENT_DISPLAY_PERIOD_LIMIT = 60` 和 `FINANCIAL_STATEMENT_SYNC_LIMIT = 60`，明确区分“展示 60 个报告期”和“同步时每类报表采集 60 期”。
- `financials/evidence-pack` 展示接口也改为按最近 60 个报告期构建，避免概览仍被 60 条原始记录截断。

当前本地数据验证：
- 对 `600519.SH` 调用 `GET /api/companies/4/financials?period_limit=60&period_offset=0` 返回 `total=240`、`count=240`、`periods=60`，最早报告日为 `2011-09-30 00:00:00`。

边界确认：
- 008 分析师快照未改动，仍由 `MAX_FINANCIAL_SNAPSHOT_PERIODS = 40` 控制，直接从数据库按最近 40 个财务报告期读取，并保留每个报告期下全部 `statement_type`。
- 因此 005 前端展示窗口是 60 个报告期；008 分析师输入窗口仍是 10 年约 40 个报告期。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\companies.py app\api\routes\companies.py app\tests\test_companies.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`13 passed, 42 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`30 passed, 1 warning`。
- `cd frontend; npm test -- --run` 通过，`42 passed`。

## 2026-08-15 财务详情按报告期折叠展示

用户反馈 005 财务详情在修复为 60 个报告期、240 条记录后页面过长。本次只调整前端展示，不改变后端取数、入库结构或传给 008/010 的财务窗口。

展示规则：
- Financials 仍然请求最近 `60` 个报告期，并保留每个报告期下全部分表记录。
- 前端不再把 240 张报表卡片一次性全部展开。
- 财务列表按 `period` 分组，每个报告期显示一行标题，标题中展示该期包含几张表以及表类型。
- 默认只展开最新报告期；点击某个报告期标题可展开该期下的全部报表，再次点击可收起。
- 展开后的同一报告期内部继续保持顺序：`main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`。
- 每条报表卡片仍保留中文字段、金额大单位显示、来源和右下角 `#id`。

实现位置：
- `frontend/src/app/CompanyWorkspaceView.tsx`：`FinancialsPanel` 新增报告期分组、展开状态和展开/收起交互；新增 `groupFinancialStatementsByPeriod(...)` helper。
- `frontend/src/styles/global.css`：新增报告期列表、报告期标题行和移动端折行样式。
- `frontend/src/tests/App.test.tsx`：更新财务展示测试，验证旧报告期默认收起、点击展开后显示该期金额、再次点击后收起。

边界确认：
- 005 数据请求仍是 `period_limit=60`，不退回原始记录 `limit=60`。
- 008 分析师快照仍保持最近 `40` 个报告期，不受本次前端折叠展示影响。

本次验证：
- `cd frontend; npm test -- --run` 通过，`42 passed`。
- `cd frontend; npm run build` 通过。
