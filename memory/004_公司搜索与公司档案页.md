# 004_公司搜索与公司档案页

## 2026-08-15 公司档案加载失败与搜索页新增表单修复

用户反馈：

1. 公司档案加载失败。
2. 搜索公司时仍显示“新增公司”表单。

排查结果：

- 当前前端代理和后端健康检查正常。
- `GET /api/companies/25` 等有效公司基础档案接口返回 `200`。
- 当前运行中的后端对 `GET /api/companies/25/investment-memos/latest` 和 `GET /api/companies/25/investment-memos` 返回 `404`，而 `CompanyWorkspaceView` 初始化使用单个 `Promise.all` 同时等待公司基础档案、财务、公告、Evidence、分析视角、Memo 等数据，导致 Memo 404 被放大成整页“公司档案加载失败”。
- `GET /api/companies/1` 返回 `Company not found`，当前数据库里 id=1 已失效；从搜索页重新选择有效公司可避免 stale company id。

本次修复：

- `CompanySearchView`：有搜索关键词时隐藏固定“新增公司”表单；无匹配结果文案改为“清空搜索后可以新增”。
- `CompanyWorkspaceView`：公司基础档案 `getCompany` 保持关键请求；财务、公告、Evidence、模型配置、分析视角、Memo 等附属模块加载失败时使用空数据兜底，不再拖垮公司档案页。
- 新增前端测试：
  - 搜索公司时隐藏新增公司面板，并确认 `q/limit/offset` 请求参数正确。
  - Memo 接口 404 时公司基础档案仍可显示。

验证结果：

- `frontend`：`npm test -- --run` 通过，38 个测试全部通过。
- `frontend`：`npm run build` 通过。
- 已重启前端开发服务，当前访问地址为 `http://127.0.0.1:5174/`；`/api/health` 代理检查返回 `200`。

## 2026-08-14 续做记录

- 本窗口续做：补齐公司搜索页分页体验，把 `GET /api/companies` 已有的 `limit/offset` 合约接到前端 UI。
  - `CompanySearchView` 新增本地 `offset` 状态，搜索关键词变化时回到第一页；上一页/下一页按钮会按 20 条分页重新请求公司池。
  - 公司搜索页顶部结果摘要现在显示当前页范围，例如“公司池共 21 家，第 1-20 家”。
  - 新增 `company-pagination`、`pagination-button`、`pagination-status` 样式，窄屏下分页按钮自动拉伸。
  - 前端测试新增“公司搜索结果翻页”用例，覆盖第一页、下一页、上一页请求参数。
  - 验证结果：`frontend` 下 `npm test -- --run` 通过，33 个测试全部通过；`npm run build` 通过。
- 已按本文件合约复核 004 相关实现入口：公司搜索、新增公司、公司档案读取、档案刷新、市场快照字段、失效公司回到搜索页。
- 验证结果：
  - `frontend`：`npm test` 通过，32 个测试全部通过。
  - `frontend`：`npm run build` 通过，TypeScript 构建和 Vite 生产构建成功。
  - `backend`：使用 `backend/.venv/Scripts/python.exe -m pytest app/tests/test_companies.py` 通过，40 个 004 相关测试全部通过。
  - `backend`：使用 `backend/.venv/Scripts/python.exe -m pytest` 通过，85 个后端测试全部通过。
  - `backend`：使用 `backend/.venv/Scripts/python.exe -m ruff check app` 通过。
- 注意：直接用系统 Python 跑后端测试会缺少 `httpx`，需要使用 `backend/.venv/Scripts/python.exe`。

## 核心职责

这个模块负责“公司主数据入口”和“单家公司档案入口”。

- 提供本地公司池搜索和列表展示。搜索范围来自本地 SQLite 中的 `Company` 主数据，不是联网搜索。
- 提供手动新增公司主数据的入口。当前搜索页在没有选中公司时固定显示“新增公司”表单；列表里没有的公司需要先通过该表单写入本地公司池。
- 提供公司档案页入口。基础档案展示公司简介、上市日期、东八区档案更新时间，以及公司档案展示用的实时基本信息：市值、价格、TTM市盈率、动态市盈率、静态市盈率、市净率、市销率、TTM股息率。用户从公司列表或新增成功结果进入单家公司工作台，后续财务、公告、外部证据、分析视角等模块都以 `company_id` 作为上下文。
- 提供公司基础档案更新入口。用户在公司档案页点击“更新”时，后端刷新缺失上市日期、公司简介和市场快照，并把结果写回本地公司主数据。
- 维护前端当前视图状态。当前没有使用显式路由库，`App.tsx` 用 `activeView` 和 `selectedCompanyId` 在 Dashboard、公司搜索页、公司档案页之间切换。
- 处理失效公司入口。公司档案读取返回 `Company not found` 时，前端清空当前选中公司并回到公司搜索页。

004 的职责边界只到公司身份、公司基础档案、档案页市场快照展示和进入单家公司工作台；财务同步、公告同步、外部证据搜索、分析视角生成是后续模块挂在公司档案页上的功能，不属于 004 的核心职责。市场快照只用于档案展示，不作为内在价值估值或分析师视角的默认输入。

## 对外接口（API/合约）

### 数据结构：`CompanyCreate`

`POST /api/companies` 的请求体。

```json
{
  "ticker": "600519.SH",
  "exchange": "SSE",
  "name": "贵州茅台",
  "industry": "白酒",
  "description": "A股白酒公司。",
  "listed_date": "2001-08-27",
  "status": "未研究",
  "tags": ["A股", "白酒", "消费"]
}
```

字段合约：

- `ticker`：必填，字符串，长度 1-32。证券代码，建议使用 `600519.SH`、`00700.HK`、`AAPL.US` 这类带市场后缀的唯一代码。
- `exchange`：必填，字符串，长度 1-32。交易所，例如 `SSE`、`SZSE`、`HKEX`、`NASDAQ`、`NYSE`。
- `name`：必填，字符串，长度 1-255。公司名称。
- `industry`：可选，字符串或 `null`，最长 120。所属行业。
- `description`：可选，字符串或 `null`。公司简介或研究备注。
- `listed_date`：可选，日期字符串或 `null`，格式 `YYYY-MM-DD`。
- `status`：可选，字符串，最长 40，默认 `未研究`。研究状态。
- `tags`：可选，字符串数组，默认 `[]`。标签会参与搜索。

### 数据结构：`CompanyRead`

公司读出接口统一返回该结构。

```json
{
  "id": 5,
  "ticker": "000858.SZ",
  "exchange": "SZSE",
  "name": "五粮液",
  "industry": "白酒",
  "description": "宜宾五粮液股份有限公司(...真实简介...)",
  "listed_date": "1998-04-27",
  "status": "未研究",
  "tags": ["A股", "白酒", "消费"],
  "market_cap": 291586393335.6,
  "current_price": 75.12,
  "pe_ttm": 23.14,
  "pe_dynamic": 9.04,
  "pe_static": 32.56,
  "pb_ratio": 2.47,
  "ps_ratio": 6.5,
  "dividend_yield_ttm": 0.06865928115015975,
  "dividend_yield_static": 0.06863684711308005,
  "market_data_source": "eastmoney_quote_snapshot,eastmoney_bonus_financing",
  "market_data_source_url": "https://...",
  "market_data_updated_at": "2026-08-14T00:48:29.428509+08:00",
  "created_at": "2026-08-09T02:42:02.384519+08:00",
  "updated_at": "2026-08-14T01:11:21.275540+08:00"
}
```

字段合约：

- `id`：整数，本地公司数据表主键。
- `ticker`：字符串，证券代码。
- `exchange`：字符串，交易所。
- `name`：字符串，公司名称。
- `industry`：字符串或 `null`。
- `description`：字符串或 `null`。公司简介。
- `listed_date`：日期字符串或 `null`。
- `status`：字符串，研究状态。
- `tags`：字符串数组。
- `market_cap`：数字或 `null`，总市值，单位元。
- `current_price`：数字或 `null`，最新价。
- `pe_ttm`：数字或 `null`，TTM 市盈率。
- `pe_dynamic`：数字或 `null`，动态市盈率。
- `pe_static`：数字或 `null`，静态市盈率。
- `pb_ratio`：数字或 `null`，市净率。
- `ps_ratio`：数字或 `null`，市销率。
- `dividend_yield_ttm`：数字或 `null`，TTM 股息率，以小数表示。
- `dividend_yield_static`：数字或 `null`，静态股息率，以小数表示。
- `market_data_source`：字符串或 `null`。
- `market_data_source_url`：字符串或 `null`。
- `market_data_updated_at`：ISO 时间字符串或 `null`，东八区。
- `created_at`：ISO 时间字符串，东八区。
- `updated_at`：ISO 时间字符串，东八区。

### `GET /api/companies`

公司列表和本地搜索。

请求参数：

- `q`：可选字符串。按 `ticker`、`name`、`exchange`、`industry`、`description`、`tags` 模糊搜索。
- `limit`：可选整数，范围 1-100，默认 20。
- `offset`：可选整数，最小 0，默认 0。

响应结构：`CompanyListResponse`

```json
{
  "items": [/* CompanyRead[] */],
  "total": 1,
  "limit": 20,
  "offset": 0
}
```

字段含义：

- `items`：`CompanyRead[]`，当前页公司列表。
- `total`：整数，符合当前搜索条件的总数。
- `limit`：整数，本次分页大小。
- `offset`：整数，本次分页偏移。

### `POST /api/companies`

手动新增公司主数据。

请求体：`CompanyCreate`

成功响应：

- HTTP `201`
- 返回 `CompanyRead`

错误响应：

- HTTP `409`：同一个 `ticker + exchange` 已存在。
- HTTP `422`：请求字段校验失败。

去重规则：

- 以后端规范化后的 `ticker.upper() + exchange.upper()` 为唯一身份。
- 数据库层也有 `ticker + exchange` 唯一约束。

### `GET /api/companies/{company_id}`

读取单家公司基础档案。

路径参数：

- `company_id`：整数，本地公司 ID。

成功响应：

- HTTP `200`
- 返回 `CompanyRead`

错误响应：

- HTTP `404`：公司不存在，`detail` 为 `Company not found`。

额外行为：

- 如果公司 `listed_date` 为空，后端会尝试通过东方财富公司概况数据源补齐上市日期；补齐失败不会阻塞公司档案读取。
- 如果公司 `description` 为空，或者仍然是“真实公司主数据种子 / 不包含实时行情或投资建议”这类占位文案，后端会尝试通过东方财富公司概况数据源补齐真实简介；补齐失败不会阻塞公司档案读取。

### `POST /api/companies/{company_id}/profile/refresh`

刷新公司基础档案展示数据。

路径参数：

- `company_id`：整数，本地公司 ID。

成功响应：

- HTTP `200`
- 返回更新后的 `CompanyRead`

刷新行为：

- 如果 `listed_date` 为空，先尝试通过东方财富公司概况补齐上市日期。
- 如果 `description` 为空或仍是占位文案，先尝试通过东方财富公司概况补齐真实简介。
- 通过东方财富行情快照刷新 `market_cap`、`current_price`、`pe_ttm`、`pe_dynamic`、`pe_static`、`pb_ratio`、`ps_ratio`、`market_data_source`、`market_data_source_url`、`market_data_updated_at`。行情主接口不可用时使用 `push2delay.eastmoney.com` 备用接口。
- 通过东方财富 F10 分红融资数据计算 `dividend_yield_ttm` 和 `dividend_yield_static`。
- 外部源某一项指标缺失时，对应字段保持 `null`，前端显示“待更新”。

错误响应：

- HTTP `400`：证券代码暂不支持行情快照刷新。
- HTTP `404`：公司不存在，`detail` 为 `Company not found`。
- HTTP `502`：外部行情源请求失败或返回不可用数据。

### 前端接口合约

`frontend/src/services/api.ts` 暴露给 004 使用的函数：

- `getCompanies(params)`：调用 `GET /api/companies`。
  - 入参：`{ q?: string; limit?: number; offset?: number; signal?: AbortSignal }`
  - 返回：`Promise<CompanyListResponse>`
- `createCompany(payload)`：调用 `POST /api/companies`。
  - 入参：`NewCompanyPayload`
  - 返回：`Promise<Company>`
- `getCompany(companyId, signal?)`：调用 `GET /api/companies/{company_id}`。
  - 入参：`companyId: number`
  - 返回：`Promise<Company>`
- `refreshCompanyProfile(companyId)`：调用 `POST /api/companies/{company_id}/profile/refresh`。
  - 入参：`companyId: number`
  - 返回：`Promise<Company>`

`CompanySearchView` 对父组件的合约：

- `selectedCompanyId: number | null`：当前是否已选中公司；为 `null` 时显示固定“新增公司”表单。
- `onSelectCompany(companyId: number): void`：选择列表公司或新建成功后进入公司档案。
- `refreshToken: number`：外部刷新信号，变化时重取公司列表。

`CompanyWorkspaceView` 对父组件的合约：

- `companyId: number | null`：当前公司档案上下文。
- `onBackToSearch(): void`：返回公司搜索页。
- `onCompanyUnavailable(): void`：公司不存在时清空选中状态并回到搜索页。
- `refreshToken: number`：外部刷新信号，变化时重新加载档案数据。
- 组件内“更新”按钮会调用 `refreshCompanyProfile`，成功后只替换当前 `company` 对象，不重置已加载的财务、公告、证据和分析数据。

## 核心依赖

### 后端依赖

- `backend/app/api/routes/companies.py`
  - 定义公司搜索、公司新增、公司详情读取、基础档案刷新路由。
- `backend/app/schemas/company.py`
  - 定义 `CompanyCreate`、`CompanyRead`、`CompanyListResponse`。
- `backend/app/services/companies.py`
  - 提供 `list_companies`、`get_company`、`get_company_by_identity`、`create_company`、`refresh_company_profile`、`refresh_company_market_snapshot`。
- `backend/app/db/models.py`
  - `Company` ORM 模型，`ticker + exchange` 唯一约束。
- `backend/app/db/session.py`
  - FastAPI 依赖注入使用的 SQLAlchemy `Session`。
- `backend/app/db/init_db.py`
  - 初始化真实公司主数据种子，并在重启时保留已经补齐的真实简介。
- `backend/app/data_sources/eastmoney_company_profile.py`
  - 公司详情读取时用于补齐上市日期和公司简介。
- `backend/app/data_sources/eastmoney_market_snapshot.py`
  - 公司基础档案刷新时用于获取市场快照。

### 前端依赖

- `frontend/src/services/api.ts`
  - 004 的 API client 和 TypeScript 数据结构来源。
- `frontend/src/app/App.tsx`
  - 维护 `activeView`、`selectedCompanyId`、`refreshToken`。
- `frontend/src/app/CompanySearchView.tsx`
  - 公司搜索、公司列表、固定新增公司表单、保存后进入档案。
- `frontend/src/app/CompanyWorkspaceView.tsx`
  - 公司档案入口和基础档案展示。
- `frontend/src/styles/global.css`
  - 公司搜索列表、公司档案页、按钮和表单样式。
- `lucide-react`
  - 搜索、公司、进入档案、更新等按钮图标。

## 2026-08-15 公司工作台分区导航

本次只优化前端组织方式，不变更公司搜索、公司新增、公司详情和基础档案刷新 API。

前端状态变化：

- `App.tsx` 维护 `activeCompanySection`，公司工作台默认进入 `overview`。
- 侧边栏 `Financials`、`Announcements`、`Analyst Views`、`Memo` 均会进入 `company-workspace` 并切换到对应分区。
- `Company Workspace` 入口进入 `overview`；从公司搜索页点击“档案”也进入 `overview`。
- 未选中公司时点击上述侧边栏模块仍进入空工作台，提示先到 `Company Search` 选择研究对象。

公司工作台分区：

- `Overview`：研究准备度。
- `Financials`：财务底稿。
- `Announcements`：公告摘要。
- `Evidence`：外部证据。
- `Analyst Views`：多视角分析。
- `Memo`：综合备忘录准备区。

页面结构边界：

- 公司顶部身份区、标签和基础档案摘要仍常驻，避免模块切换后丢失研究对象上下文。
- 财务、公告、Evidence、分析师视角不再同时堆叠在一个长页面，而是通过工作台分区切换查看。
- 本次不新增路由库，不引入新 UI 框架，继续沿用 React state 管理当前视图。

本次前端验证：

- `cd frontend; npm test -- --run` 通过，`33 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 Evidence 侧边栏入口补齐

用户复核第一阶段前端后指出缺少外部信息侧边栏入口。本次将 `Evidence` 补为公司工作台同级导航入口。

前端状态变化：

- `navigationItems` 新增 `Evidence`。
- `navigationViewMap` 新增 `Evidence -> company-workspace / evidence`。
- 侧边栏点击 `Evidence` 会保留当前选中公司上下文并打开外部证据分区；未选中公司时仍沿用空工作台提示选择公司。

本次前端验证：

- `cd frontend; npm test -- --run` 通过，`34 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 公司档案加载失败运行态排查

用户反馈前端显示“公司档案加载失败”。本次仅重启前端并检查 004 相关运行态，没有改动公司搜索或公司档案代码。

排查结果：

- 前端 `http://127.0.0.1:5174` 返回 HTTP `200`。
- 后端健康检查 `http://127.0.0.1:8001/api/health` 返回 `ok`。
- 前端代理健康检查 `http://127.0.0.1:5174/api/health` 返回 `ok`。
- `GET http://127.0.0.1:5174/api/companies/1` 返回 HTTP `404`。
- 当前公司列表仍可读取，`GET /api/companies?limit=10&offset=0` 返回 `total=26`，但列表前 10 个公司 id 不包含 `1`。

结论：

- 此次“公司档案加载失败”更像是前端当前选中或用户尝试进入的公司 id 在当前数据库中不存在，而不是前端服务或 API 代理断连。
- 重启前端后应从 `Company Search` 重新选择有效公司进入档案。
