# 008_分析师视角引擎

## 模块定位

008 是“分析师视角引擎”，用于让不同投资框架基于同一家公司当前已入库的数据快照，生成相互独立、可追溯、结构化的分析师视角。

本模块只做分析师视角分析，不做外部搜索、数据采集或第三方拉取。所有输入必须来自系统已经入库的数据。

当前核心边界：

- 每个 `analyst_view` run 完全独立，不读取、不参考、不继承历史 run。
- 每个分析师 Profile 独立运行一次，单个失败不影响其他 Profile。
- 允许读取快照中已经入库的价格、市值、估值倍数、目标价、持仓成本等字段或文本。
- 允许按分析师框架讨论安全边际、估值纪律、价格与市值相关事实。
- 不因为 `price_sensitive=true` 自动剔除外部 Evidence，也不对财务、公告、外部证据做价格脱敏。
- 不声称读取快照以外的信息，不生成搜索 query，不要求联网搜索。
- 模型输出必须是结构化 JSON，并通过 Profile、规则覆盖和引用有效性校验。

## 当前分析师 Profile

当前注册 10 个 Profile：

| id | 中文名 | 英文名 | 规则 |
| --- | --- | --- | --- |
| `buffett` | 巴菲特 | Warren Buffett | 护城河、盈利质量、管理层、安全边际 |
| `peter_lynch` | 彼得林奇 | Peter Lynch | 可理解业务、成长空间、故事与数字、资产负债风险 |
| `munger` | 芒格 | Charlie Munger | 多元模型、激励机制、企业文化、不碰清单 |
| `duan_yongping` | 段永平 | Duan Yongping | 生意质量、本分文化、消费者心智、股东回报 |
| `graham` | 格雷厄姆 | Benjamin Graham | 资产保护、盈利稳定性、保守假设、估值纪律 |
| `fisher` | 费雪 | Philip Fisher | 长期成长、创新能力、销售执行、管理深度 |
| `lin_yuan` | 林园 | Lin Yuan | 刚性需求、品牌壁垒、现金创造、复利能力 |
| `li_lu` | 李录 | Li Lu | 能力圈、研究充分度、内在价值线索、永久损失 |
| `ray_dalio` | 瑞达利欧 | Ray Dalio | 宏观敏感度、周期位置、信用流动性、组合风险信号 |
| `george_soros` | 乔治索罗斯 | George Soros | 反身性、叙事偏差、宏观脆弱点、反方证据 |

定义位置：

- `backend/app/analysis/analyst_profiles.py`

## 输入快照

快照构建入口：

- `backend/app/services/analyst_service.py`
- `build_company_analysis_snapshot(...)`

快照允许来源：

- `financial_statements`
- `announcements`
- `external_evidence`
- `evidence`：兼容旧字段名，含义等同 `external_evidence`

禁止动作：

- `web_search`
- `data_collection`
- `third_party_fetch`

当前不读取：

- `history_runs`
- 历史 `analyst_view` 结论
- 历史 `evidence_search` 结论
- 任何未入库外部信息

## 输入数量与排序

财务数据：

- 按财务期间倒序读取最近 40 条。
- 写入快照字段：`financial_statements`。
- 财务字段不再删除价格、市值、目标价、持仓成本等 key。

公告数据：

- 先读取最近 80 条公告作为候选池。
- 再按公告类型、会计口径、治理、分红回购、重大事项、关键事实、正文摘录等可复核信号排序。
- 最终保留 20 条。
- 公告排序不读取公告 `importance_score`。
- 公告标题、摘要、关键事实、正文摘录、标签不做价格脱敏。

外部证据：

- 先读取最近 60 条 Evidence 作为候选池。
- 再按重要性和可信度排序，最终保留 10 条。
- 最终保留 10 条。
- `price_sensitive=true` 的 Evidence 可以进入 008。
- 传给分析师模型的外部证据只保留 `id`、`title`、`published_at`、`source_type`、`summary` 五个字段。
- 外部证据的完整字段仍保留在后端 Evidence 库中，用于前端展示、历史管理和后续排查。

快照中会记录：

- `data_counts.financial_statements`
- `data_counts.announcements`
- `data_counts.external_evidence`
- `data_counts.announcement_candidates`
- `data_counts.external_evidence_candidates`
- `data_counts.price_sensitive_external_evidence`
- `source_boundary.snapshot_limits`
- `source_boundary.selection_policy`
- `source_boundary.price_sensitive_policy`

## Fact Ledger

008 在调用模型前会构建 `fact_ledger`，用于把当前快照的事实基础和可解释评分集中记录下来。

主要字段：

- `analysis_basis`：本次使用的财务期间、公告 id、外部证据 id、会计事件数量和说明。
- `profile_relevance`：分析师框架与公司类型的适配度，由服务层规则计算。
- `data_confidence`：本次快照的数据质量和完整度，由服务层规则计算。
- `financial_quality`：财务期数、报表类型、可比性和现金流信息概览。
- `external_evidence_quality`：外部证据数量、结构化状态、待复核数量和可信度概览。
- `accounting_events`：会计政策、会计估计、差错更正、追溯调整、收入确认、核算方式变化、非经常性因素等事件。

写入 run 前，服务层会用 `fact_ledger` 覆盖模型输出中的：

- `profile_fit_score`
- `confidence`
 - `analysis_basis`
 - `accounting_events`
 - `score_explanations`
  - 外部证据的模型输入字段已收敛为 5 个字段，避免把 `importance_score`、`credibility_score`、`use_scope`、`analysis_note` 等 007 评分和复核字段直接带入分析师视角。

含义：

- `profile_fit_score` 是分析师框架与公司类型的适配度，不是看多/看空强度。
- `confidence` 是当前快照数据质量，不是观点正确概率。

## Prompt 与输出结构

Prompt 定义位置：

- `backend/app/analysis/prompts/analyst.py`
- `PROMPT_VERSION = "analyst_view_v1"`

Prompt 当前要求：

- 只能使用用户消息中的数据快照。
- 每次 run 完全独立，不读取、参考或延续历史 run。
- `evidence / external_evidence` 指 007 外部信息模块已入库记录，不等同财务报表或公告本身。
- 如果快照中包含当前价格、历史价格、目标价、市值、估值倍数、持仓成本等信息，可以按当前分析师框架正常读取、引用和判断，但必须说明依据来自快照。
- 可以围绕商业质量、管理层、护城河、成长质量、风险、反方证据、估值纪律、安全边际和估值假设建议展开。
- 如果 `fact_ledger.accounting_events` 非空，评价收入、利润或同比变化前必须说明会计口径、收入确认、追溯调整或非经常性因素影响。
- 证据不足时写入 `data_gaps` 或 `follow_up_questions`。

结构化输出字段：

- `analyst_profile`
- `overview`
- `profile_fit_score`
- `confidence`
- `key_observations`
- `rule_checks`
- `supporting_evidence_ids`
- `financial_observations`
- `announcement_observations`
- `risk_flags`
- `counter_evidence`
- `valuation_assumption_suggestions`
- `data_gaps`
- `follow_up_questions`
- `analysis_basis`
- `accounting_events`
- `score_explanations`

`rule_checks[].status` 只能使用：

- `pass`
- `warn`
- `fail`
- `unknown`

Schema 会兼容部分模型输出别名，例如：

- `reasoning / assessment / conclusion` -> `summary`
- `id` -> `rule_id`
- 常见非标准 status 会归一到 `pass / warn / fail / unknown`
- `bear_case` -> `counter_evidence`
- `valuation_assumptions` -> `valuation_assumption_suggestions`

Schema 定义位置：

- `backend/app/schemas/analysis.py`

## 运行流程

单个分析师运行入口：

- `run_company_analyst_view(...)`

流程：

1. 校验 Profile 是否存在。
2. 构建当前公司、当前 Profile 的输入快照。
3. 计算 `data_snapshot_hash`。
4. 创建 `AnalysisRun(status="running")`。
5. 调用 `ModelGateway.generate_structured(...)`。
6. 用 `fact_ledger` 覆盖评分和依据字段。
7. 校验输出 Profile、规则覆盖和引用有效性。
8. 成功时覆盖同公司同 Profile 的当前 `analyst_view` 记录，写入 `status="success"`、`result`、`confidence`、`is_latest=true`。
9. 失败时同样覆盖同公司同 Profile 的当前 `analyst_view` 记录，写入 `status="failed"`、`result.error`、`result.error_type`、`is_latest=true`；错误文本会做清洗和压缩。

当前 `parent_run_id` 固定为空，避免 run 之间形成继承关系。

批量运行入口：

- `run_company_analyst_views_batch(...)`

行为：

- 请求为空数组时默认运行全部 Profile。
- 可以指定部分 Profile。
- 每个 Profile 独立生成。
- 单个 Profile 失败不会阻断其他 Profile。
- 每个 Profile 最多保留一条最新 `AnalysisRun`；新分析会覆盖同公司同 Profile 的旧记录。

## 引用校验

写入 success 前会校验：

- `analyst_profile` 必须等于请求 Profile。
- `rule_checks` 必须覆盖当前 Profile 的全部规则。
- `supporting_evidence_ids` 必须来自当前快照中的 `external_evidence/evidence id`。
- `rule_checks[].evidence_ids` 必须来自当前快照中的 `external_evidence/evidence id`。
- `rule_checks[].announcement_ids` 必须来自当前快照中的公告 id。
- `rule_checks[].financial_periods` 必须来自当前快照中的财务期间。

引用无效时：

- 当前 run 写入 `status="failed"`。
- `result.error_type="ModelOutputValidationError"`。

## Run 记录策略

008 使用通用 `analysis_runs` 表。

固定标识：

- `run_type="analyst_view"`
- `run_version="008_v1"`
- `prompt_version="analyst_view_v1"`

记录策略：

- 每个公司、每个分析师 Profile 只保留一条最新 `analyst_view` run。
- 新分析启动时复用同公司同 Profile 的最新记录，并删除该 Profile 其他旧 `analyst_view` 记录。
- 新分析成功时覆盖为 `status="success"`、`is_latest=true`。
- 新分析失败时覆盖为 `status="failed"`、`is_latest=true`，用于前端展示最新失败原因。
- 删除只能由用户通过删除接口显式触发；删除后该 Profile 暂无分析记录。
- 历史 run 不再作为 008 的持久保留目标，也不进入下一次 008 输入。

## API

当前接口：

- `GET /api/analyst-profiles`
- `GET /api/companies/{company_id}/analysis/runs/latest`
- `GET /api/companies/{company_id}/analysis/runs`
- `POST /api/companies/{company_id}/analysis/runs`
- `POST /api/companies/{company_id}/analysis/runs/batch`
- `DELETE /api/companies/{company_id}/analysis/runs/{run_id}`

查询 run 支持：

- `run_type`
- `analyst_profile`
- `status`
- `limit`
- `offset`

生成请求示例：

```json
{
  "analyst_profile": "buffett",
  "user_note": "可选备注"
}
```

批量生成请求示例：

```json
{
  "analyst_profiles": ["buffett", "munger"],
  "user_note": "可选备注"
}
```

## 前端行为

公司工作台的分析师视角区域会展示：

- 全部分析师 Profile。
- 每个 Profile 的中文名、英文名、简介和规则 chips。
- 最新成功 run。
- 最近失败 run。
- 数据置信度。
- 视角适配度。
- 关键观察。
- 本次分析依据。
- 评分解释。
- 会计口径提示。
- 规则检查结果。
- 财务观察。
- 公告观察。
- 风险、缺口、反方证据、估值假设建议。
- 后续问题。

操作：

- 单个 Profile 生成。
- 批量生成全部或多个 Profile。
- 生成中可停止前端等待请求。
- 可显式删除指定 run 记录。

时间显示：

- 分析师 run 的生成时间统一按东八区 `Asia/Shanghai` 显示。
- 后端如果返回 SQLite 常见的无时区时间字符串，例如 `2026-08-09 00:40:00.000000`，前端按 UTC 解析后再转换为东八区显示。
- 带 `Z` 或显式时区偏移的 ISO 时间按其原始时区语义解析，再转换为东八区显示。

注意：

- 停止按钮取消的是当前前端 HTTP 等待；如果后端已经完成处理，仍可能写入已经开始的 run。
- 删除 run 是真实删除数据库记录，只在用户显式点击时发生。

## 当前验证命令

后端测试应在 `backend/` 目录执行：

```powershell
cd backend
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m pytest
```

前端测试和构建：

```powershell
cd frontend
npm test -- --run
npm run build
```

最近一次相关验证结果：

- `cd backend; .\.venv\Scripts\python.exe -m ruff check .`：通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest`：72 passed，1 warning。
- `cd frontend; npm test -- --run`：32 passed。
- `cd frontend; npm run build`：通过。

本地快照复核示例：

- `600036.SH 招商银行`
- `buffett` 视角
- `financial_statements=40`
- `announcements=20`
- `external_evidence=9`，因为当前库中外部证据候选只有 9 条，上限是 10。
- 快照文本中允许出现 `市值` 和 `持仓成本`。

## 2026-08-14

- 外部证据输入已收敛为 `id`、`title`、`published_at`、`source_type`、`summary` 五个字段，避免把 `importance_score`、`credibility_score`、`use_scope`、`analysis_note` 直接带入分析师视角。
- 证据选择按重要性 + 可信度排序后最多取 10 条，不再先按发布时间截断到固定候选池。
- 前端分析师视角卡片已补充展示 `key_observations`、`financial_observations`、`announcement_observations` 和 `follow_up_questions`，避免结构化结果字段只入库不展示。
- 本次前端验证：`cd frontend; npm test -- --run` 通过，32 passed；`cd frontend; npm run build` 通过。
- 本次后端相关验证：`cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，22 passed，1 warning。另一次 `cd backend; .\.venv\Scripts\python.exe -m ruff check .` 命中既有非 008 长行：`app\db\migrations.py:178`，未在本次 008 范围内修改。

## 2026-08-14 财务证据包接入

008 已接入 005 生成的结构化 `financial_evidence_pack`，用于让分析师模型更高效地使用财务指标。该证据包不是 007 外部 Evidence，也不会进入 `evidence_ids`。

输入快照变化：

- `build_company_analysis_snapshot(...)` 现在按 `fields.report_date`、`period`、`id` 倒序读取最近 40 条财务记录。
- 快照顶层同时保留 `financial_statements` 和 `financial_evidence_pack`。
- `financial_statements` 继续负责追溯原始期间记录。
- `financial_evidence_pack` 负责向模型提供已整理的财务事实、比率指标、趋势指标、财务旗标和数据缺口。
- `data_counts` 增加 `financial_flags` 和 `financial_data_gaps` 数量。
- `source_boundary.selection_policy` 已说明财务排序口径为 `fields.report_date`、`period`、`id` 倒序。

Prompt 使用方式：

- `backend/app/analysis/prompts/analyst.py` 已明确说明：`financial_evidence_pack` 是后端确定性财务证据包，`financial_statements` 是原始期间记录。
- 分析师应优先从证据包提取 `financial_observations`、`risk_flags`、`data_gaps` 和 `valuation_assumption_suggestions`。
- 财务证据引用仍然使用 `rule_checks[].financial_periods`，例如 `2025年报`、`2025A`；不得把财务期间或财务指标写入 `evidence_ids`。
- 当证据包显示缺少资本开支、自由现金流、有息负债、现金、分红、回购或股本等关键字段时，分析师应把这些写入 `data_gaps` 或后续估值假设复核问题。

Fact ledger 变化：

- `fact_ledger.financial_quality` 会读取 `financial_evidence_pack.latest_period`。
- `fact_ledger.financial_quality.flags_count` 记录财务旗标数量。
- `fact_ledger.financial_quality.data_gaps` 记录财务数据缺口。
- 如果证据包存在缺口，会给出“现金流、资产负债或股东回报字段缺失会降低估值可比性”的可比性提示。
- 服务层仍覆盖 `profile_fit_score`、`confidence`、`analysis_basis`、`score_explanations`，避免模型自由打分。

和 010 估值实验室的衔接：

- 008 只输出财务质量判断、风险识别和估值假设建议，不直接生成买卖建议。
- 010 后续应读取同一份 005 `financial_evidence_pack` 派生 `valuation_inputs`，并可参考 008 的 `valuation_assumption_suggestions`。
- 010 的估值计算底座应来自 005 的确定性财务事实和指标，例如基准收入、基准净利润、基准自由现金流、成长率、利润率、ROIC、资本开支、现金、有息负债、分红、回购和股本。
- 010 阶段保持 `price_blind=true`，不得读取当前价格、市值、历史价格、持仓成本、评级或市场情绪；价格对照留给 011。

本次验证：

- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" app\tests\test_analysis.py -k "financial or snapshot or analyst_view_records"` 通过，`18 passed`。
- 触达后端文件 ruff 通过：`financial_metrics.py`、`companies.py`、`analyst_service.py`、`company.py`、`companies.py` 路由、`analyst.py` prompt、相关测试文件。
- `cd backend; .\.venv\Scripts\python.exe -m pytest` 未全量通过，失败点在 007 Evidence 搜索兜底统计用例，非本次 008 财务证据包路径。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check .` 未全量通过，失败点在既有 `app/services/evidence_service.py` 两条 E501 长行，非本次 008 财务证据包路径。
- `cd frontend; npm test -- --run` 通过，`32 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-14 前置证据梳理与优化草案

当前 008 分析师视角读取的前置证据口径：

- 005 财务：读取本地已入库 `financial_statements` 最近 40 条，同时读取后端确定性整理的 `financial_evidence_pack`。证据包包含 `latest_period`、`periods`、财务事实、盈利/现金/成长/资产负债/效率/每股指标、趋势、财务旗标和数据缺口。财务引用使用 `rule_checks[].financial_periods`，不写入 `evidence_ids`。
- 006 公告：读取本地已入库公告候选池，按公告类型、会计口径、治理、分红回购、重大事项、关键事实、正文摘要等可复核信号排序，最终最多传 20 条。公告来自同步、快速摘要或深度摘要后的当前字段，用 `rule_checks[].announcement_ids` 引用。
- 007 外部信息：读取本地 Evidence，按重要性和可信度排序后最多传 10 条。传给模型的外部证据只保留 `id`、`title`、`published_at`、`source_type`、`summary`，避免把 007 的评分和复核字段直接带入分析师视角。`price_sensitive=true` 仍可进入 008，但必须按快照证据说明。
- 008 自建事实账本：调用模型前构建 `fact_ledger`，记录分析依据、视角适配度、数据置信度、财务质量、外部证据质量和会计事件；写入 run 前由服务层覆盖评分、分析依据、会计事件和评分解释。
- 008 不读取历史 `analyst_view`、历史 `evidence_search` 结论，也不触发联网搜索、数据采集或第三方拉取。

优化草案：

- 建议新增 `source_coverage_matrix`：在快照和 `fact_ledger` 中按财务、公告、外部 Evidence 三类记录覆盖主题，例如盈利质量、现金流、资产负债、治理、监管、行业供需、消费者/渠道、宏观周期、估值相关字段和价格敏感字段，帮助模型知道哪些判断有证据、哪些只是缺口。
- 建议把公告快照从 5 字段升级为 7 字段：在现有 `id/title/published_at/category/summary` 基础上补入 `key_facts`、`tags`，与 006 的后续分析师证据字段一致，提升公告对规则检查和会计事件识别的可用性。
- 建议区分外部 Evidence 的 `model_analyzed` 与 `search_lead`：模型输入仍保持 5 字段，但可在 `fact_ledger.external_evidence_quality` 和 `analysis_basis.notes` 中明确 search_lead 数量与限制，避免分析师把追溯线索当成已验证事实。
- 建议新增确定性 `pre_model_observations`：服务层从 `financial_evidence_pack.financial_flags`、`financial_data_gaps`、公告会计事件和 Evidence 质量中生成短句，作为模型分析前的事实提示，不替代模型观点。
- 建议改进引用校验到“覆盖质量”层：除了校验 id 和 period 是否存在，还可统计每条规则是否至少引用一种相关证据；允许 `unknown` 规则不强制引用，但要求写入缺口或后续问题。
- 建议增加快照版本摘要：在 `input_snapshot` 中记录 `financial_evidence_pack` 摘要/hash、公告候选数、Evidence 候选数和最终入模数，便于复盘为什么本次分析和下一次不同。
- 建议把 008 输出的 `valuation_assumption_suggestions` 进一步结构化为 `assumption_type`、`reason`、`needed_inputs`、`source_refs`，后续 010 估值实验室可以更稳定地消费。

## 2026-08-14 前置证据优化落地与评分口径 v2

本次按前置证据优化草案继续推进 008。

已落地内容：

- 快照顶层新增 `source_coverage_matrix`，并同步写入 `fact_ledger.source_coverage_matrix`。
- `source_coverage_matrix` 按来源拆分 `financial`、`announcement`、`external_evidence`，并按主题记录覆盖情况，例如 `profitability`、`cash_flow`、`balance_sheet`、`growth`、`governance`、`regulatory`、`industry_supply_demand`、`consumer_channel`、`accounting`、`capital_return`、`major_event`、`price_sensitive`、`data_gaps`。
- 快照顶层新增 `pre_model_observations`，并同步写入 `fact_ledger.pre_model_observations`。该字段由服务层确定性生成，用于提示财务旗标、财务数据缺口、search_lead 限制、会计口径事件和核心主题缺口，不是模型结论。
- 公告输入继续包含 `id`、`title`、`published_at`、`category`、`summary`、`key_facts`、`tags` 七个字段。
- Prompt 已要求模型阅读 `source_coverage_matrix` 和 `pre_model_observations`，不要把覆盖矩阵缺失的主题写成确定性结论。
- 估值假设保持兼容：`valuation_assumption_suggestions` 仍是给前端展示的中文短句列表；新增 `valuation_assumption_details`，结构为 `assumption_type`、`reason`、`needed_inputs`、`source_refs`，供后续 010 估值实验室消费。
- 前端已兼容展示 `valuation_assumption_details`，旧的纯字符串估值假设展示不受影响。

关于数据置信度和视角适配度的修正：

- 原问题：旧版 `profile_fit_score` 和 `confidence` 容易被理解成精确概率，且权重过粗；`profile_fit_score` 还会被当前证据多少过度影响，导致“公司类型明明适合某个框架，但证据不足时分数被错杀”。
- 新口径：保留 0-1 总分以兼容 UI，但在 `score_explanations` 中增加 `tier`、`score_type`、`components`、`limitations`。
- `profile_fit_score` 现在使用 `method="rule_based_company_profile_fit_v2"`，含义是 `framework_applicability_estimate`，主要看公司/行业与分析师框架是否匹配，同时参考该框架核心主题的当前覆盖情况。
- `profile_fit_score.components` 包含：
  - `industry_framework_fit`
  - `rule_topic_coverage`
  - `evidence_support`
  - `uncertainty_penalty`
- `confidence` 现在使用 `method="snapshot_quality_v2"`，含义是 `source_coverage_quality_estimate`，只表示当前快照底稿质量，不表示观点正确概率。
- `confidence.components` 包含：
  - `financial_coverage`
  - `announcement_coverage`
  - `external_evidence_quality`
  - `source_balance`
  - `accounting_complexity_penalty`
- 关键拆分：`profile_fit_score` 更偏“这个分析框架适不适合这家公司”；`confidence` 更偏“当前证据底稿够不够支撑分析”。二者不再混成一个模糊判断。

本次验证：

- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`23 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\schemas\analysis.py app\analysis\prompts\analyst.py app\tests\test_analysis.py` 通过。
- `cd frontend; npm test -- --run` 通过，`33 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-14 前端后续问题标点修复

- 修复分析师视角卡片中 `follow_up_questions` 复用通用分号拼接导致的 `？；` 标点混乱。
- 新增前端格式化函数：后续问题会去掉每条问题末尾已有的 `。/；/?/？`，统一补中文问号，并用空格分隔，例如 `问题一？ 问题二？`。
- 保持风险、缺口、反方、估值假设等字段继续使用原有分号拼接。
- 本次验证：`cd frontend; npm test -- --run` 通过，`33 passed`；`cd frontend; npm run build` 通过。

## 2026-08-14 前端分析依据展示优化

- 后端 008 实际入模仍按快照上限读取：`financial_statements` 最近 40 条、`announcements` 最多 20 条、`external_evidence/evidence` 最多 10 条；如果本地库不足对应数量，则按实际入库数量使用。
- `analysis_basis` 仍由服务层 `fact_ledger` 覆盖并保留完整财务期间、公告 id 和外部证据 id 列表，用于追溯本次实际依据。
- 前端“本次分析依据”不再只显示前几项导致误解为只分析了这些，而是显示总量 + 少量预览：
  - `财务期 5 期（展示 4 期：...）`
  - `公告 6 条（展示 5 条：...）`
  - `外部证据 6 条（展示 5 条：...）`
- 这样既不把 40/20/10 全部铺满页面，也能明确告诉用户模型实际依据列表比前端预览更完整。
- 本次验证：`cd frontend; npm test -- --run` 通过，`33 passed`；`cd frontend; npm run build` 通过。

## 2026-08-15 分析师 run 覆盖策略

- 用户要求：所有分析师都只保留最新一次分析记录，新分析直接覆盖之前的记录。
- 已实现：`run_company_analyst_view(...)` 创建 running run 时，会优先查找同公司、同 Profile 的最新 `analyst_view` run 并复用该记录；如果存在同 Profile 其他旧 run，会删除这些旧 run。
- 成功和失败都会覆盖同一条最新记录：
  - 成功：`status="success"`、`result`、`confidence`、`is_latest=true`。
  - 失败：`status="failed"`、`result.error`、`result.error_type`、`confidence=None`、`is_latest=true`。
- 列表查询和 latest 查询对旧库中的重复 `analyst_view` 记录做防御性去重：先按同 Profile 最新记录归并，再应用 `status` 过滤，避免“最新失败后旧成功仍作为最新成功显示”。
- 非 `analyst_view` 类型的 `analysis_runs` 不受本次覆盖策略影响。
- 本次新增测试覆盖：同公司同 Profile 存在多条旧 run 时，新分析复用最新记录并删除其他旧记录；其他 Profile 的记录不受影响。
- 本次验证：`cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`24 passed, 1 warning`。
- 本次触达 lint：`cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。

## 2026-08-15 前端 Analyst Views 分区与 Memo 准备输入

本次只调整前端展示组织，不改动 008 后端分析师 run 生成、覆盖、查询或删除逻辑。

变化：

- 分析师视角面板从公司工作台长页面中收敛到 `Analyst Views` 分区。
- 侧边栏 `Analyst Views` 可直接打开当前公司的分析师分区；未选中公司时先进入空工作台提示选择公司。
- `Overview` 研究准备度会基于当前已加载 Profile、成功 run 和失败 run 计算成功数量、失败数量和缺失 Profile 数量。
- 新增前端 `Memo` 准备区会读取 008 的成功 `analyst_view` run，汇总 `risk_flags`、`counter_evidence`、`data_gaps`、`valuation_assumption_suggestions`，作为 009 前置素材展示。
- `Memo` 准备区只消费当前前端已加载的 008 数据，不触发新的 009 后端生成能力。

本次前端验证：

- `cd frontend; npm test -- --run` 通过，分析师单个生成、批量生成、失败 run 展示、删除和停止请求用例均已更新到 `Analyst Views` 分区。
- `cd frontend; npm run build` 通过。

## 2026-08-15 最新失败视角展示修复

- 针对用户在贵州茅台中遇到的情况：段永平视角先失败，随后重新生成成功，但 009 Memo 准备区仍显示“段永平 最近失败”。
- 后端 `list_latest_company_analysis_runs(..., run_type="analyst_view", status="failed")` 已补充测试：同一 profile 如果较新的记录为 success，则旧 failed 不应进入 latest failed 结果。
- 前端 `buildResearchReadiness` 和 `buildMemoPreparation` 增加防御过滤：同一 profile 只要已经在当前成功 run 列表中，就不会计入最近失败数量，也不会显示“最近失败”标签。
- 本次验证：`cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`26 passed, 1 warning`；`cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过；`cd frontend; npm test -- --run` 通过，`40 passed`；`cd frontend; npm run build` 通过。

## 2026-08-15 基础档案估值字段与现金流覆盖修正

用户以贵州茅台巴菲特视角输出复核后指出两类问题：
- 安全边际分析没有读取基础档案里的当前价格、市值、TTM 市盈率、动态市盈率、静态市盈率、市净率、市销率、TTM 股息率等字段。
- `data_gaps` 写“缺少经营现金流绝对值”容易被理解为完全没有现金流数据，但 005 财务数据已存在每股经营现金流、经营现金流/收入等现金流代理指标。

本次 008 修正：
- `_company_snapshot(company)` 正式把基础档案行情估值字段写入分析师快照：`market_cap`、`current_price`、`pe_ttm`、`pe_dynamic`、`pe_static`、`pb_ratio`、`ps_ratio`、`dividend_yield_ttm`、`dividend_yield_static`、`market_data_source`、`market_data_source_url`、`market_data_updated_at`。
- Prompt 明确提示模型优先检查 `company` 中的基础档案行情估值字段；如果使用价格、市值或估值倍数，需说明依据来自快照。
- `profile_fit_score` 的现金流信号不再只看 `operating_cash_flow` 绝对值，也会识别 `operating_cash_flow_per_share` 和 `operating_cash_flow_to_revenue`，避免有现金流代理指标时低估巴菲特、林园、段永平等现金创造视角的适配度。
- 008 继续不主动联网、不触发第三方抓取，只读取当前已入库快照；基础档案行情估值字段来自 `companies` 表，不来自未入库外部信息。

与 005 的衔接：
- 005 `financial_evidence_pack` 新增 `cash_flow_coverage`，并把“缺经营现金流绝对值”分成完全缺失和已有代理口径两种文案。
- 008 的 `data_gaps` 可以继续使用 005 缺口，但模型应把“已有代理口径但缺绝对值”理解为严格估值前的补充项，而不是完全没有现金流数据。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`26 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_companies.py -k "financial" -q` 通过，`9 passed, 39 deselected, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\services\financial_metrics.py app\analysis\prompts\analyst.py app\tests\test_analysis.py app\tests\test_companies.py` 通过。

## 2026-08-15 财务字段补齐后的分析师输入优化方案

用户确认 005 需要采集资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本等字段。008 在字段补齐后，应优化传给分析师的财务输入方式，避免把原始财务 JSON 直接堆给模型。

建议保持双层输入：

- `financial_statements`：继续保留最近 40 条原始期间记录，用于追溯和 `rule_checks[].financial_periods` 引用。
- `financial_evidence_pack`：作为分析师优先阅读的确定性财务证据包。

建议新增或强化的分析师友好字段：

- `financial_evidence_pack.analyst_summary`：服务层生成的短结构摘要，包含最新期间、收入/利润/现金流规模、盈利质量、现金质量、资产负债安全、股东回报和主要风险。
- `financial_evidence_pack.quality_matrix`：按主题组织覆盖情况和判断素材，例如 `profitability`、`cash_flow`、`growth`、`balance_sheet`、`capital_allocation`、`shareholder_return`。
- `financial_evidence_pack.valuation_readiness`：告诉分析师哪些估值方法已具备基本输入，例如 DCF 是否已有自由现金流、分红折现是否已有分红、剩余收益是否已有股东权益和 ROE、资产价值法是否已有现金/负债/资产质量字段。
- `financial_evidence_pack.capital_allocation`：集中放资本开支、分红、回购、股本变化，用于判断管理层资本配置和股东回报。
- `financial_evidence_pack.balance_sheet_adjustment`：集中放现金、有息负债、净现金、资产负债率、现金/有息负债，用于后续估值资产负债调整。
- `financial_evidence_pack.cash_flow_quality`：集中放经营现金流、资本开支、自由现金流、经营现金流/净利润、自由现金流/净利润、自由现金流率。
- `financial_evidence_pack.data_quality`：把缺口从字符串升级为结构化对象，并标注是否已有代理口径，避免模型把“缺严格绝对值”误读成“完全没有数据”。

建议传给模型的压缩策略：

- 对 008 入模内容，优先传 `latest`、3 年摘要、5 年摘要和趋势结论，不把所有历史字段完整铺开。
- 原始 `financial_statements` 可以保留，但模型 prompt 应明确：除非需要追溯具体期间，否则优先使用 `financial_evidence_pack`。
- 对每个财务观察附带 `source_periods`，例如 `["2025年报", "2024年报"]`，方便模型写入 `rule_checks[].financial_periods`。
- `pre_model_observations` 应从财务旗标、数据缺口、估值准备度和现金流质量中生成短句，作为模型分析前的确定性提示。

建议面向 010 的衔接：

- 008 不直接计算内在价值，也不直接给买卖建议。
- 008 输出 `valuation_assumption_details` 时，可引用 `valuation_readiness` 和 `cash_flow_quality`，说明哪些估值假设可以用，哪些必须等 010 补充或人工复核。
- 010 后续应直接读取 005 的 `financial_evidence_pack` 生成 `valuation_inputs`，008 的作用是提供假设建议和风险提示，不替代估值计算。

## 2026-08-15 财务字段补齐 goal 开发 prompt

已为后续 goal 模式准备开发 prompt，要求在 005 补齐资本开支、自由现金流、有息负债、货币资金、分红、回购、总股本等估值关键字段后，008 继续保留 `financial_statements + financial_evidence_pack` 双层输入，并优先让分析师读取 `analyst_summary`、`quality_matrix`、`cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness`、`data_quality` 等分析师友好字段。

## 2026-08-15 财务证据包 v2 接入记录

本次在 005 完成估值关键财务字段补齐后，同步优化 008 的分析师输入消费方式。008 仍只读取当前已经入库的快照，不主动联网搜索，不触发第三方采集；财务证据继续来自 `financial_statements` 和 `financial_evidence_pack`。

输入快照继续保留双层结构：
- `financial_statements`：最近 40 条原始期间财务记录，用于追溯具体期间和写入 `rule_checks[].financial_periods`。
- `financial_evidence_pack`：分析师优先阅读的后端确定性财务证据包，用于形成财务观察、风险、数据缺口和估值假设建议。

本次接入强化：
- `source_coverage_matrix.financial` 现在可以识别 `cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness`，并覆盖 `cash_flow`、`balance_sheet`、`capital_allocation`、`shareholder_return`、`valuation_inputs` 等主题。
- `pre_model_observations` 会从财务旗标、结构化缺口、估值准备度和现金流覆盖情况生成确定性提示，提醒模型哪些是服务层事实提示，哪些仍需要补充。
- `fact_ledger.financial_quality` 增加或强化 `structured_gaps_count`、`cash_flow_coverage`、`valuation_readiness`、`balance_sheet_safety`、`shareholder_return_coverage`、`data_quality`。
- `backend/app/analysis/prompts/analyst.py` 已明确要求模型优先读取 `financial_evidence_pack` v2 的 `analyst_summary`、`quality_matrix`、`cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness` 和 `data_quality`。
- Prompt 已明确禁止模型从原始财务 JSON 现场猜公式；自由现金流、净现金、现金/有息负债、经营现金流/净利润、自由现金流/净利润、分红率、回购率等指标必须以 005 后端确定性计算结果为准。
- 如果分析师基于 `valuation_readiness` 或 `cash_flow_quality` 形成后续估值假设，要求同步写入 `valuation_assumption_details`，并用 `source_refs.financial_periods` 追溯财务期间。

对后续 010 的衔接：
- 008 只给“估值假设建议”和“需要复核的输入”，不计算内在价值，不给买卖动作。
- 010 后续生成 `valuation_inputs` 时应直接读取 005 的 `financial_evidence_pack`，008 的 `valuation_assumption_details` 只作为假设和风险提示，不替代确定性财务底稿。
- 010 仍必须保持 `price_blind=true`，不得把当前价格、市值、历史价格、持仓成本、评级或市场情绪放入无锚定估值输入。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check .` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`26 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m pytest` 通过，`117 passed, 1 warning`。
- `cd frontend; npm test -- --run` 通过，`40 passed`。
- `cd frontend; npm run build` 通过。
## 2026-08-15 财务快照 40 期口径修正

本次修正 008 传给分析师的财务记录截断口径，配合 005 已经把财务拆成主财务指标、现金流量表、资产负债表等多种 `statement_type` 的结构。

新的分析师输入规则：
- `financial_statements` 仍作为原始财务记录输入，用于追溯具体期间和写入 `rule_checks[].financial_periods`。
- 截断上限从“最近 40 条记录”改为“最近 40 个财务期间”。
- 对被选中的每个期间，保留该期间下所有 `statement_type`，避免三张表拆分后把十年历史压缩成三年多。
- `source_boundary.snapshot_limits` 记录为 `financial_statement_periods: 40`。
- `data_counts` 新增或强化 `financial_statement_periods`，用于说明本次实际覆盖多少个财务期间；`financial_statements` 仍表示实际传入的原始记录条数。

示例口径：
- 如果每期只有 1 条主财务指标，最近 40 期仍对应 40 条 `financial_statements`。
- 如果每期有主财务指标、现金流量表、资产负债表 3 条记录，最近 40 期会对应约 120 条 `financial_statements`。
- 分析师 prompt 仍要求优先阅读 `financial_evidence_pack`，原始 `financial_statements` 主要用于追溯和必要复核。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`27 passed, 1 warning`。

## 2026-08-15 旧快照复核与前端标点展示修正

用户以贵州茅台巴菲特视角 #1552 为例指出：
- “本次分析依据”仍显示财务期 14 期。
- 估值假设等位置出现 `。，` 这类连续标点。
- “本次分析依据”中 `会计口径事件 3 个1 条外部信息...` 存在文本黏连。

复核结果：
- 本地贵州茅台财务记录已有 `180` 条、`60` 个 distinct 财务期间，每期包含 `main_financial_indicators`、`cash_flow_statement`、`balance_sheet` 三张表。
- #1552 的 `input_snapshot` 是旧口径持久化结果：`financial_statements=40`，distinct 财务期间只有 `14`，且没有 `financial_statement_periods` 字段。
- 使用当前代码即时构建贵州茅台巴菲特快照，结果为 `financial_statements=120`、`financial_statement_periods=40`，`fact_ledger.analysis_basis.financial_periods` 也是 `40`。因此 #1552 需要重新生成分析师视角后才会使用新快照。

本次前端修正：
- `Analyst Views` 展示层新增轻量文本清洗，去除 `。，`、重复句号/问号/分号/逗号等展示噪声。
- `valuation_assumption_details.reason` 末尾标点会先剥离，再拼接 `，需补充 ...`，避免出现 `影响同比口径。，需补充`。
- “本次分析依据”和评分解释的多个展示项之间保留明确空格，避免复制或窄布局时出现 `3 个1 条`、`判断数据依据` 这类黏连。
- 该修正只改变前端展示，不篡改模型原始 JSON 和已保存 run 的结构化结果。

本次验证：
- `cd frontend; npm test -- --run` 通过，`40 passed`。
- `cd frontend; npm run build` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`27 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。

## 2026-08-15 利润表加入后的分析师输入字段调整

用户询问如果 005 后续新增利润表，传入后续分析师的字段是否需要修改。判断如下：原始 `financial_statements` 通道不需要重做，但 `financial_evidence_pack` 和 008 快照中的分析师友好字段需要扩展，否则利润表只会成为原始 JSON，模型不能稳定利用利润构成。

008 需要配合的变化：
- `financial_statements` 继续按“最近 40 个财务期间”选取，并保留每个期间下全部 `statement_type`。利润表加入后，40 期可能对应约 160 条原始财务记录，不应按 40 条记录截断。
- `source_boundary.selection_policy.financial_statements` 文案需要补充 `income_statement`，说明每个期间保留主要财务指标、利润表、现金流量表、资产负债表等分表记录。
- `source_coverage_matrix.financial` 建议新增或识别 `profit_structure`、`expense_control`、`accounting_quality` 主题。
- `fact_ledger.financial_quality` 建议增加 `income_statement_coverage`，记录是否有利润表、是否有费用明细、是否有减值和非经常性项目。
- `pre_model_observations` 应能读取利润表相关 `financial_flags`，提示模型关注费用率上升、非主营收益占比高、减值损失扩大、扣非利润弱于归母利润等确定性信号。
- 分析师 prompt 需要提示优先读取 `financial_evidence_pack` 中的 `profit_structure`、`expense_control`、`income_statement_quality` 或 `profit_composition`，不要从原始利润表 JSON 现场猜公式。

推荐 005 提供给 008 的新增结构化字段：
- `financial_metrics.profit_structure`：营业利润率、营业利润/净利润、扣非净利润/归母净利润、投资收益/净利润、公允价值变动/净利润、减值损失/净利润、营业外收支/净利润、有效税率。
- `financial_metrics.expense_control`：销售费用率、管理费用率、研发费用率、财务费用率、期间费用率。
- `quality_matrix.profit_structure`、`quality_matrix.expense_control`、`quality_matrix.accounting_quality`。
- `analyst_summary.income_statement_quality` 或 `analyst_summary.profit_composition`。
- `financial_data_gaps` 中加入利润表明细缺口；当主要财务指标已提供收入和净利润时，标记为有替代结果项但缺少构成明细。

这样 008 的分析师可以直接回答“利润质量是否可靠、利润是否来自主营、费用纪律是否变差、估值利润基准是否需要正常化调整”，而不是只看到收入、净利润和现金流覆盖率。

## 2026-08-15 利润表补齐后的财务输入字段清单

利润表补齐后，008 分析师接收的财务输入保持 `financial_statements + financial_evidence_pack + fact_ledger/pre_model_observations` 三个使用层次。

原始追溯层 `financial_statements[]`：
- 每条记录包含 `id`、`period`、`statement_type`、`currency`、`fields`、`source`、`source_url`、`created_at`。
- `statement_type` 至少包含 `main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`。
- `fields` 按报表类型保存原始精细值，不做前端“亿/万”展示缩写。

分析师优先使用层 `financial_evidence_pack`：
- 顶层：`latest_period`、`periods`、`financial_facts`、`financial_metrics`、`cash_flow_coverage`、`financial_trends`、`financial_flags`、`financial_data_gaps`、`financial_data_gap_messages`、`cash_flow_quality`、`balance_sheet_adjustment`、`capital_allocation`、`valuation_readiness`、`quality_matrix`、`analyst_summary`、`data_quality`。
- `financial_facts.latest/series`：收入、毛利、净利润、扣非净利润、经营现金流、资本开支、自由现金流、现金、有息负债、净现金、总资产、总负债、股东权益、分红、回购、总股本，以及利润表补充的营业成本、费用、投资收益、公允价值变动、减值损失、营业利润、利润总额、所得税、少数股东损益等利润构成字段。
- `financial_metrics`：`profitability`、`profit_structure`、`expense_control`、`cash_quality`、`growth_quality`、`balance_sheet_safety`、`efficiency`、`per_share`、`shareholder_return`、`capital_allocation`。
- `financial_trends`：收入/净利润/自由现金流 CAGR，ROE/毛利率/净利率稳定性，资本开支/收入趋势，有息负债趋势，分红稳定性，股本趋势，以及营业利润率、费用率、研发费用率、财务费用率、投资收益占比、减值损失占比、有效税率等利润表趋势。
- `quality_matrix`：`profitability`、`profit_structure`、`expense_control`、`accounting_quality`、`cash_flow`、`growth`、`balance_sheet`、`capital_allocation`、`shareholder_return`、`valuation_inputs`。
- `analyst_summary`：最新期间、业务规模、盈利能力、利润构成/利润表质量、现金流质量、资产负债安全、资本配置、股东回报、主要财务旗标和主要数据缺口。

008 快照辅助层：
- `fact_ledger.financial_quality` 应包含 `periods`、`latest_period`、`statement_types`、`comparability_notes`、`flags_count`、`structured_gaps_count`、`data_gaps`、`cash_flow_coverage`、`valuation_readiness`、`balance_sheet_safety`、`shareholder_return_coverage`、`data_quality`，利润表补齐后再增加 `income_statement_coverage`。
- `source_coverage_matrix.financial` 应覆盖 `profitability`、`profit_structure`、`expense_control`、`accounting_quality`、`cash_flow`、`growth`、`balance_sheet`、`capital_allocation`、`shareholder_return`、`valuation_inputs`、`data_gaps`。
- `pre_model_observations` 应包含由财务旗标、利润表质量、现金流质量、估值准备度和数据缺口生成的确定性提示。

## 2026-08-15 利润表适配分步开发 prompt 方案

本次为后续 goal 模式准备第二段 008 适配指令。执行顺序要求：先完成 005 利润表采集和 `financial_evidence_pack` 字段扩展，再执行 008 适配。

008 prompt 重点边界：
- 008 不主动联网、不触发第三方采集，只读取已经入库的公司、财务、公告和外部 Evidence 快照。
- `financial_statements` 仍按最近 40 个财务期间选取，并保留每个期间全部 `statement_type`；利润表加入后不能退回按 40 条原始记录截断。
- 分析师应优先消费 `financial_evidence_pack`，尤其是新增的 `profit_structure`、`expense_control`、`accounting_quality`、`income_statement_quality/profit_composition`。
- 008 只输出质量判断、风险、反方证据和估值假设建议，不计算内在价值，不给买卖动作。
- 修改完成后更新本文件并跑分析师相关后端/前端测试。

## 2026-08-15 规则结论直接校正覆盖

用户确认：`通过/观察/未知/不通过` 不需要做双轨审计覆盖，前端人工调整应直接校正当前分析结果，因为用户只会修正明显由模型漏读数据导致的错误判断。

本次实现规则：
- `rule_checks[].status` 继续只使用规范值：`pass`、`warn`、`unknown`、`fail`。
- 前端 `Analyst Views` 中每条规则的状态从静态标签改为下拉菜单，可直接选择“通过/观察/未知/不通过”。
- 保存时调用 `PATCH /api/companies/{company_id}/analysis/runs/{run_id}/rule-checks/{rule_id}`。
- 后端只更新当前 `analyst_view` run 的 `result.rule_checks[]` 中匹配 `rule_id` 的 `status`，不新增 shadow/original 字段，也不保留旧状态。
- 更新后前端用后端返回的 run 替换当前列表中的同一条 run，不重新生成分析、不触发 005/006/007 数据刷新。
- 因为 009 综合投资备忘录和 010 无锚定估值实验室后续会读取 008 的 `rule_checks`，人工校正后的状态会自然进入后续备忘录和估值前置判断。

本次触达文件：
- `backend/app/schemas/analysis.py`
- `backend/app/services/analyst_service.py`
- `backend/app/api/routes/analysis.py`
- `backend/app/tests/test_analysis.py`
- `frontend/src/services/api.ts`
- `frontend/src/app/CompanyWorkspaceView.tsx`
- `frontend/src/styles/global.css`
- `frontend/src/tests/App.test.tsx`

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过：`29 passed, 1 warning`。
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\schemas\analysis.py app\services\analyst_service.py app\api\routes\analysis.py app\tests\test_analysis.py` 通过。
- `cd frontend; npm test -- --run` 通过：`42 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 利润表结构化字段正式适配

本次在 005 已补齐 `statement_type="income_statement"` 和 `financial_evidence_pack` 利润表字段后，完成 008 分析师输入适配。边界保持不变：008 不主动联网，不触发第三方采集，只读取已入库公司、财务、公告和外部 Evidence 快照；008 不计算内在价值，不给买入、卖出、持有、减仓等交易动作；010 无锚定估值实验室仍保持 `price_blind=true`。

财务快照口径：
- `financial_statements` 继续按最近 40 个财务期间选取。
- 每个期间继续保留全部 `statement_type`。
- 利润表加入后，40 期可对应约 160 条原始财务记录，不退回按 40 条记录截断。
- `data_counts.financial_statement_periods` 表示财务期间数。
- `data_counts.financial_statements` 表示实际传入的原始财务记录条数。

source_boundary 更新：
- `source_boundary.selection_policy.financial_statements` 已说明每个期间保留主要财务指标、利润表（`income_statement`）、现金流量表、资产负债表以及其他后续扩展分表记录。

source_coverage_matrix 更新：
- `source_coverage_matrix.financial` 现在能基于 `financial_evidence_pack` 识别并标记：
  - `profitability`
  - `profit_structure`
  - `expense_control`
  - `accounting_quality`
  - `cash_flow`
  - `growth`
  - `balance_sheet`
  - `capital_allocation`
  - `shareholder_return`
  - `valuation_inputs`
  - `data_gaps`
- `profit_structure`、`expense_control`、`accounting_quality` 优先来自 `financial_metrics`、`quality_matrix`、`income_statement_quality` 和 `profit_composition`，不依赖模型理解原始 JSON。

fact_ledger 更新：
- `fact_ledger.financial_quality` 新增 `income_statement_coverage`。
- `income_statement_coverage` 包含：
  - `has_income_statement`
  - `has_expense_breakdown`
  - `has_operating_profit`
  - `has_impairment_items`
  - `has_non_operating_items`
  - `has_tax_expense`
  - `missing_fields`
  - `note`
- 原有字段继续保留：`periods`、`latest_period`、`statement_types`、`comparability_notes`、`flags_count`、`structured_gaps_count`、`data_gaps`、`cash_flow_coverage`、`valuation_readiness`、`balance_sheet_safety`、`shareholder_return_coverage`、`data_quality`。

pre_model_observations 更新：
- 008 会从 005 已计算的利润表类 `financial_flags` 生成服务层确定性提示，不重新计算公式。
- 已覆盖的提示包括：
  - 投资收益占净利润较高，需要复核利润可持续性。
  - 期间费用率连续上升，费用纪律需要关注。
  - 财务费用率上升，融资成本或杠杆压力需要关注。
  - 减值损失占净利润较高，需要复核资产质量。
  - 扣非净利润显著弱于归母净利润，需要识别非经常性利润贡献。
- 如果结构化利润表字段可用，会提示模型优先使用 `financial_evidence_pack`，不要从原始 JSON 现场猜公式。

Prompt 更新：
- `backend/app/analysis/prompts/analyst.py` 已明确要求：如果存在 `financial_evidence_pack`，优先读取结构化财务证据包。
- 利润表补齐后优先读取：
  - `financial_metrics.profit_structure`
  - `financial_metrics.expense_control`
  - `quality_matrix.accounting_quality`
  - `analyst_summary.income_statement_quality`
  - `analyst_summary.profit_composition`
- 明确禁止从原始 `income_statement` JSON 现场猜公式。
- 利润表相关观察引用财务期间时写入 `rule_checks[].financial_periods` 或 `valuation_assumption_details[].source_refs.financial_periods`，不要写入 `evidence_ids`。
- 分析师只能输出财务质量判断、风险、反方证据和估值假设建议，不计算内在价值，不给交易动作。

测试更新：
- `backend/app/tests/test_analysis.py` 已把 40 期分表测试升级为四类 `statement_type`，验证 40 个财务期间保留约 160 条原始记录。
- 新增利润表结构化字段快照测试，验证：
  - `source_boundary` 文案包含利润表和 `income_statement`。
  - `source_coverage_matrix.financial` 覆盖 `profit_structure`、`expense_control`、`accounting_quality`。
  - `fact_ledger.financial_quality.income_statement_coverage` 存在并标记利润表、费用明细、营业利润、减值、营业外项目和所得税覆盖。
  - `pre_model_observations` 包含利润表相关确定性提示。
  - prompt 包含优先读取结构化利润表字段、不要从原始 JSON 猜公式、财务期间引用位置和禁止交易动作的要求。

本次验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\analysis\prompts\analyst.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过：`30 passed, 1 warning`。
- `cd frontend; npm test -- --run` 通过：`42 passed`。
- `cd frontend; npm run build` 通过。

## 2026-08-15 005 财务详情 60 期展示修复后的 008 边界确认

本次 005 财务详情修复将前端 Financials 展示窗口改为最近 `60` 个报告期，并保留每期全部分表记录；这是 005 展示层和展示用 evidence pack 的窗口，不改变 008 分析师快照输入规则。

008 当前规则保持不变：
- `backend/app/services/analyst_service.py` 中 `MAX_FINANCIAL_SNAPSHOT_PERIODS = 40` 继续作为分析师财务输入窗口。
- 008 直接从数据库读取公司财务记录，按最近 `40` 个不同财务报告期筛选。
- 对被选中的每个报告期，继续保留全部 `statement_type`，包括 `main_financial_indicators`、`income_statement`、`cash_flow_statement`、`balance_sheet`。
- 在四张表结构下，008 最近 40 个报告期可对应约 `160` 条原始 `financial_statements` 记录，仍是 10 年左右财务历史。

验证：
- `cd backend; .\.venv\Scripts\python.exe -m ruff check app\services\analyst_service.py app\tests\test_analysis.py` 通过。
- `cd backend; .\.venv\Scripts\python.exe -m pytest app\tests\test_analysis.py -q` 通过，`30 passed, 1 warning`。

## 2026-08-16 008-010 估值信号源重构方向

用户提出：010 无锚定估值实验室不应再主要依赖 009 memo 或关键词推断分析师结论，而应直接使用 008 各分析师四条分析规则的判定结果（通过/观察/不通过/未知）作为估值参数影响指标。

008 后续职责调整：
- 每个 `analyst_view` run 除现有 `rule_checks[]` 外，应新增或派生一份 price-blind 的 `valuation_parameter_matrix`。
- `valuation_parameter_matrix` 以每位分析师的每条规则为最小单位，保存：
  - `profile_id`
  - `source_run_id`
  - `rule_id`
  - `status`
  - `status_score`
  - `price_blind_compatible`
  - 规则归属的估值维度，例如 `business_quality`、`moat_durability`、`growth_runway`、`pricing_power`、`cash_flow_reliability`、`balance_sheet_risk`、`capital_intensity`、`cyclicality`、`permanent_loss_risk`。
  - 该规则对估值参数的方向性影响，例如增长率、折现率、永续增长率、情景宽度和方法权重。
- 状态分值采用用户确认口径：`pass=+1`、`warn=-0.35`、`fail=-2`、`unknown=0`。
- 008 当前允许讨论价格和安全边际，但供 010 消费的矩阵必须 `price_blind_compatible=true`；可能受价格锚点影响的规则或文字不得进入 010 参数计算。

该调整的含义：
- 008 从“人类分析师视角输出”升级为“分析师判定 + 估值参数信号源”。
- 人工在前端校正 `rule_checks[].status` 后，应自然改变后续 010 参数建议。
- 009 综合投资备忘录只做总结记录，不再作为估值参数计算的主要信号源。

## 2026-08-16 008 valuation_parameter_matrix 正式实现

本轮已完成 008 到 010 的结构化信号源改造。

实现内容：
- 新增 `backend/app/analysis/valuation_parameter_matrix.py`，为 10 个 AnalystProfile、每人 4 条规则建立完整显式映射，共 40 条。
- 每条映射明确保存 `dimensions`、`parameter_impacts`、`calculation_role` 和默认 `price_blind_compatible`。
- 状态分值固定为 `pass=+1.00`、`warn=-0.35`、`fail=-2.00`、`unknown=0.00`，不再通过关键词推断规则状态。
- 每次 008 成功 run 保存时，在 `result.valuation_parameter_matrix` 中派生 `analyst_items[].rule_impacts[]`。
- 每条 rule impact 保存 profile/run/rule、状态与分值、维度映射、参数映射、来源引用、摘要和 price-blind 排除原因。
- `buffett.margin_of_safety` 和 `graham.valuation_discipline` 固定为 `calculation_role="price_reference"`。
- 其他规则若 summary/source_refs 命中价格、市值、估值倍数、持仓成本、评级、目标价、市场情绪或安全边际等锚点，也会在运行时转入 `price_reference`，不参与 010 计算。
- `PATCH rule_checks[].status` 保存后会立即用同一派生函数重算该 run 的 `valuation_parameter_matrix` 并返回更新后的 run。

涉及业务文件：
- `backend/app/analysis/valuation_parameter_matrix.py`
- `backend/app/services/analyst_service.py`
- `backend/app/tests/test_analysis.py`

测试覆盖：
- 映射 key 与全部 Profile 规则集合严格相等，数量为 40。
- 每个 Profile 均可派生 4 条 rule impacts。
- 四种状态分值为 `+1/-0.35/-2/0`。
- 人工 PATCH 规则状态后矩阵同步重算并持久化。

验证结果：
- 后端全量 Ruff：通过。
- 后端全量 pytest：`140 passed, 1 warning`。
- warning 为现有 Starlette/httpx deprecation。

剩余限制：
- 历史 008 run 不会批量回写矩阵；010 读取旧成功 run 时会按当前显式映射即时派生兼容矩阵。重新生成或人工 PATCH 后会正式保存到 run.result。
