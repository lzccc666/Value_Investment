# 008 分析师视角引擎

## 职责与边界

008 使用同一份已入库财务、公告与外部证据，让 8 个固定投资框架独立输出结构化判断。008 不读取当前价、历史价、市值、估值倍数、评级、目标价、持仓成本、市场情绪、安全边际或交易动作，也不生成买卖建议。

输入快照会递归删除价格锚字段，并清洗嵌套文本。模型输出会再次递归检查；只要任意字段或文本包含价格锚，整次运行失败并保存失败原因，不允许把单条规则降级后继续使用。

当前提示版本为 analyst_view_v4。提示词明确要求模型不要在最终 JSON 中复述边界说明里的禁止概念或英文技术字段名，也不能把这些内容写成“未评估”“无法判断”或数据缺口；模型必须在输出前静默删除相关整句。该提示只降低偶发复述概率，不放宽后端硬门禁，任何实际命中仍使整次运行失败。

## 固定分析师与规则

顺序、Profile ID、规则 ID 和每人四条规则是 008、010 与前端共享的稳定合同。

| 顺序 | Profile | 展示名 | 四条规则 |
| --- | --- | --- | --- |
| 1 | buffett | 巴菲特 | durable_moat 持久护城河；owner_earnings_quality 所有者收益；capital_allocation 资本配置；management_candor 管理层坦诚 |
| 2 | peter_lynch | 彼得林奇 | business_understandability 业务可理解；growth_runway 成长空间；story_numbers_alignment 叙事对账；growth_financial_resilience 成长负担 |
| 3 | munger | 查理·芒格 | multi_model_resilience 多元验证；incentive_alignment 激励一致；rational_culture 理性文化；ruin_risk_control 毁灭风险 |
| 4 | duan_yongping | 段永平 | right_business 好生意；consumer_value_mindshare 用户心智；benfen_culture 本分文化；cash_reinvestment_discipline 现金再投资 |
| 5 | graham | 本杰明·格雷厄姆 | working_capital_safety 营运安全；capital_structure_safety 资本结构；earnings_record 盈利记录；asset_accounting_quality 资产质量 |
| 6 | fisher | 菲利普·费雪 | market_runway 市场空间；innovation_productivity 创新效率；sales_customer_strength 销售与客户；management_depth 管理纵深 |
| 7 | lin_yuan | 林园 | must_have_repeat_demand 刚需复购；monopoly_brand_power 品牌垄断；cash_profitability 现金盈利；scalable_compounding 规模复利 |
| 8 | li_lu | 李录 | economic_knowability 经济可知性；moat_growth_coexistence 护城河成长；owner_governance 股东治理；permanent_loss_resilience 永久损失 |

系统不包含达里奥或索罗斯 Profile。

## 深度档案

每个 Profile 同时公开并注入提示词：

- description：该框架观察公司的入口。
- philosophy：框架的核心投资哲学。
- core_logic：关键推理原则。
- decision_sequence：固定分析顺序。
- preferred_evidence：优先采用的证据类型。
- failure_modes：常见误判方式。
- prompt_focus：模型必须重点回答的主题。
- rules：四条规则的标签、完整说明和五状态判定口径。

每条规则的 status_rubric 都包含 pass、neutral、unknown、warn、fail。unknown 的统一含义是关键证据缺失、来源冲突或口径不可比；neutral 表示证据足以判断但结论普通或无明显正负倾向。

## 运行输出

规则状态及分值合同：

| 状态 | 分值 | 含义 |
| --- | ---: | --- |
| pass | 1.0 | 明确满足 |
| neutral | 0.0 | 可判断但中性 |
| unknown | -0.1 | 证据不足 |
| warn | -0.5 | 存在重要警示 |
| fail | -2.0 | 明确不满足 |

每次成功运行保存：

- overview、key_observations、financial_observations、announcement_observations。
- 精确 4 条 rule_checks，顺序必须与 Profile 定义一致。
- risk_flags、counter_evidence、data_gaps、follow_up_questions。
- valuation_assumption_suggestions 与可追溯 source_refs。
- profile_fit_score、confidence、配置版本与完整配置快照。
- valuation_parameter_matrix：4 条规则全部 calculation_role=compute，全部 price_blind_compatible=true。

旧 parameter_impacts 和 price_reference 层已退出运行时合同。32 条规则全部参与 010 数值聚合。

## 数据与证据

分析快照只引用已入库数据：

- 公司静态档案中允许的非价格字段。
- 财务证据包、财务期间与确定性指标。
- 公告摘要及公告 ID。
- 外部 Evidence 及 Evidence ID。
- 数据缺口、来源哈希与配置快照。

规则引用只能指向快照中真实存在的 evidence_ids、announcement_ids 和 financial_periods。

## API 与前端

- GET /api/analysis/profiles：按固定顺序返回 8 个深度档案和规则 rubric。
- POST /api/companies/{company_id}/analysis/runs：运行单个 Profile。
- POST /api/companies/{company_id}/analysis/runs/batch：运行指定 Profile；空列表表示全部 8 个。
- PATCH /api/analysis/runs/{run_id}/rule-checks/{rule_id}：人工修改五状态之一，并重建该运行的规则矩阵。
- GET /api/companies/{company_id}/analysis/runs/latest：无状态筛选时读取各 Profile 最新运行；`status=success` 时读取各 Profile 最近一次成功运行，使新的运行中或失败尝试不会遮住 008 页面已有评价；`status=failed` 仍只返回当前最新记录确为失败的 Profile。

前端按固定顺序展示 8 个分析师，并展示每人最近一次成功评价、哲学、核心逻辑、决策顺序、偏好证据、失败模式、规则说明及五状态 rubric。重新生成期间保留原成功评价。

## 结构性约束

- Profile 必须恰好 8 个，每人恰好 4 条规则，总计 32 条。
- 规则矩阵必须恰好 32 条、13 个维度、117 个维度系数，全部 compute。
- 输入和输出都必须保持价格盲区。
- 价格锚输出必须使整次 008 失败，不能降级、忽略或伪造旧结果。
- 手工修改只影响目标历史运行；已保存的 009、010、011 快照不原地重算。
